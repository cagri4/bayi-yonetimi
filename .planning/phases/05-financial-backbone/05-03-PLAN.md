---
phase: 05-financial-backbone
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/lib/actions/financials.ts
  - src/components/admin/financial/transaction-form.tsx
  - src/components/admin/financial/invoice-upload.tsx
  - src/components/admin/financial/dealer-balance-card.tsx
  - src/app/admin/dealers/[id]/financials/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can create transaction (payment, adjustment) for any dealer"
    - "Admin can upload invoice PDF linked to transaction"
    - "Admin sees dealer balance overview"
    - "Audit trail records admin who created transaction"
    - "Validation prevents invalid amounts and missing fields"
  artifacts:
    - path: "src/lib/actions/financials.ts"
      provides: "Admin Server Actions for financial mutations"
      contains: "createDealerTransaction, uploadInvoice"
    - path: "src/app/admin/dealers/[id]/financials/page.tsx"
      provides: "Admin dealer financials management page"
      contains: "TransactionForm, InvoiceUpload"
  key_links:
    - from: "src/app/admin/dealers/[id]/financials/page.tsx"
      to: "src/lib/actions/financials.ts"
      via: "Server Action calls"
      pattern: "createDealerTransaction, uploadInvoice"
---

<objective>
Build admin interface for managing dealer financial transactions and invoices.

Purpose: Admins can enter payments, adjustments, and upload invoice PDFs for dealers.

Output: Admin financials page with transaction form, invoice upload, and balance overview.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-financial-backbone/05-RESEARCH.md
@.planning/phases/05-financial-backbone/05-02-PLAN.md
@src/lib/actions/
@src/app/admin/dealers/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin Server Actions</name>
  <files>src/lib/actions/financials.ts</files>
  <action>
Add admin-only Server Actions to financials.ts:

1. **createDealerTransaction(input)**: Create payment/adjustment
   - Validates admin role
   - Zod schema validation
   - Records created_by
   - Revalidates paths
   - Types: payment, credit_note, debit_note, opening_balance

2. **uploadInvoice(formData)**: Upload invoice with PDF
   - Validates admin role
   - Creates transaction (type: invoice)
   - Uploads PDF to storage
   - Creates invoice record
   - Rollback on failure
   - Revalidates paths

3. **getAdminDealerBalance(dealerId)**: Get any dealer's balance
   - Admin-only
   - Uses same RPC function

4. **getAdminDealerTransactions(dealerId, filters)**: Get any dealer's transactions
   - Admin-only
   - Same structure as dealer version

See 05-RESEARCH.md for complete implementation patterns.
  </action>
  <verify>
1. Admin functions check role
2. Non-admin calls rejected
3. Transaction created with audit trail
4. PDF uploaded to correct path
  </verify>
  <done>
Admin Server Actions added:
- createDealerTransaction() with validation
- uploadInvoice() with PDF handling and rollback
- getAdminDealerBalance() for any dealer
- getAdminDealerTransactions() for any dealer
  </done>
</task>

<task type="auto">
  <name>Task 2: Create transaction form component</name>
  <files>src/components/admin/financial/transaction-form.tsx</files>
  <action>
Create TransactionForm for admin to enter transactions:

- Form fields:
  - Transaction type dropdown (payment, credit_note, debit_note, opening_balance)
  - Amount (positive number, TRY)
  - Description (required)
  - Reference number (optional, e.g., makbuz no)
  - Transaction date (date picker)
  - Notes (optional textarea)

- Uses react-hook-form + zod
- Loading state during submission
- Success/error toast messages
- Form resets on success

Props:
```typescript
interface TransactionFormProps {
  dealerId: string
  onSuccess?: () => void
}
```
  </action>
  <verify>
1. Form validates required fields
2. Amount accepts only positive numbers
3. Submission creates transaction
4. Toast shows success/error
  </verify>
  <done>
TransactionForm component:
- All required form fields
- Zod validation
- Loading states
- Toast feedback
  </done>
</task>

<task type="auto">
  <name>Task 3: Create invoice upload component</name>
  <files>src/components/admin/financial/invoice-upload.tsx</files>
  <action>
Create InvoiceUpload for admin to upload invoice with PDF:

- Form fields:
  - Invoice number (required)
  - Invoice date (date picker)
  - Total amount (positive number)
  - Description (required)
  - Due date (optional)
  - PDF file upload (required, max 10MB)
  - Notes (optional)

- File validation: PDF only, max 10MB
- Upload progress indicator
- Uses FormData for file upload
- Success/error handling with rollback

Props:
```typescript
interface InvoiceUploadProps {
  dealerId: string
  onSuccess?: () => void
}
```
  </action>
  <verify>
1. File input accepts only PDF
2. File size validated
3. Upload creates transaction + invoice record
4. Rollback on failure
  </verify>
  <done>
InvoiceUpload component:
- Invoice form fields
- PDF file validation
- Upload progress
- Error handling with rollback
  </done>
</task>

<task type="auto">
  <name>Task 4: Create dealer balance card component</name>
  <files>src/components/admin/financial/dealer-balance-card.tsx</files>
  <action>
Create DealerBalanceCard for admin overview:

- Shows dealer name and company
- Current balance summary (same as dealer view)
- Quick stats: total transactions, last transaction date
- Link to full transaction history

Props:
```typescript
interface DealerBalanceCardProps {
  dealer: {
    id: string
    name: string
    company_name: string
  }
  balance: {
    totalDebit: number
    totalCredit: number
    netBalance: number
  }
}
```
  </action>
  <verify>
1. Card displays dealer info
2. Balance formatted correctly
3. Stats accurate
  </verify>
  <done>
DealerBalanceCard component:
- Dealer identification
- Balance summary
- Quick stats
  </done>
</task>

<task type="auto">
  <name>Task 5: Create admin financials page</name>
  <files>src/app/admin/dealers/[id]/financials/page.tsx</files>
  <action>
Create admin dealer financials page:

1. Server Component fetching:
   - Dealer info (name, company)
   - getAdminDealerBalance(dealerId)
   - getAdminDealerTransactions(dealerId, filters)

2. Layout:
   - Breadcrumb: Bayiler > [Dealer Name] > Finansal
   - DealerBalanceCard at top
   - Two-column or tabbed layout:
     - Transaction entry form
     - Invoice upload form
   - Transaction history table (reuse TransactionList)

3. Actions section with forms

4. History section with filters
  </action>
  <verify>
1. Page loads for valid dealer
2. Balance displays correctly
3. Forms submit successfully
4. Transaction history shows
  </verify>
  <done>
Admin financials page:
- Dealer balance overview
- Transaction entry form
- Invoice upload form
- Transaction history
  </done>
</task>

<task type="auto">
  <name>Task 6: Add link to dealer edit page</name>
  <files>src/app/admin/dealers/[id]/edit/page.tsx</files>
  <action>
Add "Finansal Islemler" link/button to dealer edit page:

- Button or link to /admin/dealers/[id]/financials
- Shows current balance as preview
- Positioned in actions area or as tab
  </action>
  <verify>
1. Link visible on dealer edit page
2. Navigates to financials page
3. Balance preview shows (optional)
  </verify>
  <done>
Dealer edit page updated:
- Link to financials management
- Balance preview (if implemented)
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `npm run build` passes
2. Admin can access /admin/dealers/[id]/financials
3. Transaction form creates records
4. Invoice upload stores PDF and creates records
5. Balance updates after transactions
6. Audit trail shows admin who created
7. Non-admin cannot access admin actions
</verification>

<success_criteria>
- FIN-05: Admin bayi icin cari hareket girebilir (borc/alacak/odeme) ✓
- FIN-06: Admin bayiye fatura PDF'i yukleyebilir ✓
- Audit trail records created_by for all transactions
- RLS prevents unauthorized access
</success_criteria>

<output>
After completion, create `.planning/phases/05-financial-backbone/05-03-SUMMARY.md`
</output>
