---
phase: 05-financial-backbone
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/actions/financials.ts
  - src/components/financials/balance-summary.tsx
  - src/components/financials/transaction-list.tsx
  - src/components/financials/transaction-filters.tsx
  - src/components/financials/invoice-list.tsx
  - src/app/(dealer)/financials/page.tsx
  - src/components/layout/nav-links.tsx
autonomous: true

must_haves:
  truths:
    - "Dealer sees current balance (toplam borc, alacak, net bakiye) on financials page"
    - "Dealer can browse transaction history with date filtering"
    - "Dealer can download invoice PDFs via signed URL"
    - "Dealer sees payment history in transactions"
    - "Navigation includes Cari Hesap link"
  artifacts:
    - path: "src/lib/actions/financials.ts"
      provides: "Server Actions for dealer financial queries"
      contains: "getDealerBalance, getDealerTransactions, getInvoiceDownloadUrl"
    - path: "src/app/(dealer)/financials/page.tsx"
      provides: "Dealer financials page"
      contains: "BalanceSummary, TransactionList"
  key_links:
    - from: "src/app/(dealer)/financials/page.tsx"
      to: "src/lib/actions/financials.ts"
      via: "Server Action calls"
      pattern: "getDealerBalance, getDealerTransactions"
---

<objective>
Build dealer-facing financials page for viewing cari hesap balance and transactions.

Purpose: Dealers can see their financial status, transaction history, and download invoices.

Output: Financials page with balance summary, transaction list with filters, and invoice downloads.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-financial-backbone/05-RESEARCH.md
@.planning/phases/05-financial-backbone/05-01-PLAN.md
@src/lib/actions/
@src/components/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create financials Server Actions</name>
  <files>src/lib/actions/financials.ts</files>
  <action>
Create Server Actions for dealer financial queries:

1. **getDealerBalance()**: Returns { totalDebit, totalCredit, netBalance }
   - Uses get_dealer_balance_breakdown RPC
   - Returns empty balance for unauthenticated

2. **getDealerTransactions(filters)**: Returns paginated transactions
   - Filters: startDate, endDate, typeCode, page, pageSize
   - Includes transaction_type join for display
   - Includes order join if linked

3. **getInvoiceDownloadUrl(invoiceId)**: Returns signed URL
   - Verifies dealer ownership
   - 1 hour expiration

4. **getTransactionTypes()**: Returns all active types
   - For filter dropdown

See 05-RESEARCH.md for complete implementation patterns.
  </action>
  <verify>
1. Import works without errors
2. Functions return expected types
3. RLS blocks cross-dealer access
  </verify>
  <done>
Server Actions created:
- getDealerBalance() with RPC call
- getDealerTransactions() with filters and pagination
- getInvoiceDownloadUrl() with signed URL
- getTransactionTypes() for filter options
  </done>
</task>

<task type="auto">
  <name>Task 2: Create balance summary component</name>
  <files>src/components/financials/balance-summary.tsx</files>
  <action>
Create BalanceSummary component (see 05-RESEARCH.md):

- Three cards: Toplam Borc (red), Toplam Alacak (green), Net Bakiye
- Net Bakiye shows (Borc) or (Alacak) suffix
- Uses Intl.NumberFormat for TRY formatting
- Icons: TrendingDown, TrendingUp, Wallet

Props:
```typescript
interface BalanceSummaryProps {
  totalDebit: number
  totalCredit: number
  netBalance: number
}
```
  </action>
  <verify>
1. Component renders three cards
2. Currency formatting correct (TRY)
3. Colors match balance direction
  </verify>
  <done>
BalanceSummary component:
- Three-card grid layout
- Turkish currency formatting
- Color-coded by balance direction
  </done>
</task>

<task type="auto">
  <name>Task 3: Create transaction list component</name>
  <files>src/components/financials/transaction-list.tsx</files>
  <action>
Create TransactionList component (see 05-RESEARCH.md):

- Table with columns: Tarih, Tip, Aciklama, Referans, Tutar, Islem
- Date formatting with date-fns (Turkish locale)
- Transaction type badges (color by balance_effect)
- Icons per transaction type
- Download button for invoices
- Shows due date if present

Props:
```typescript
interface TransactionListProps {
  transactions: DealerTransaction[]
  onDownloadInvoice?: (transactionId: string) => void
}
```
  </action>
  <verify>
1. Table renders with correct columns
2. Dates formatted in Turkish
3. Download button visible for invoices
  </verify>
  <done>
TransactionList component:
- Responsive table with all columns
- Turkish date formatting
- Type badges with icons
- Invoice download action
  </done>
</task>

<task type="auto">
  <name>Task 4: Create transaction filters component</name>
  <files>src/components/financials/transaction-filters.tsx</files>
  <action>
Create TransactionFilters component:

- Date range picker (startDate, endDate)
- Transaction type dropdown
- Uses URL search params for state (server-side filtering)
- Clear filters button

Props:
```typescript
interface TransactionFiltersProps {
  transactionTypes: { code: string; name: string }[]
  currentFilters: {
    startDate?: string
    endDate?: string
    typeCode?: string
  }
}
```

Use existing patterns from catalog filters.
  </action>
  <verify>
1. Filters update URL params
2. Page reloads with filters applied
3. Clear button resets filters
  </verify>
  <done>
TransactionFilters component:
- Date range inputs
- Type select dropdown
- URL-based filter state
- Clear filters action
  </done>
</task>

<task type="auto">
  <name>Task 5: Create financials page</name>
  <files>src/app/(dealer)/financials/page.tsx</files>
  <action>
Create dealer financials page:

1. Server Component fetching:
   - getDealerBalance()
   - getDealerTransactions(filters from searchParams)
   - getTransactionTypes()

2. Layout:
   - Page header: "Cari Hesap"
   - BalanceSummary at top
   - TransactionFilters
   - TransactionList
   - Pagination if needed

3. Handle invoice download:
   - Client action to get signed URL
   - Open in new tab or trigger download

4. Empty state if no transactions
  </action>
  <verify>
1. Page loads without errors
2. Balance displays correctly
3. Transactions list populated
4. Filters work
5. Invoice download works
  </verify>
  <done>
Financials page:
- Balance summary widget
- Filterable transaction list
- Invoice download functionality
- Proper empty states
  </done>
</task>

<task type="auto">
  <name>Task 6: Add navigation link</name>
  <files>src/components/layout/nav-links.tsx</files>
  <action>
Add "Cari Hesap" link to dealer navigation:

- Icon: Wallet or Receipt
- Path: /financials
- Position: After Favorilerim, before other links
- Active state styling
  </action>
  <verify>
1. Link appears in navigation
2. Active state works on /financials
3. Icon displays correctly
  </verify>
  <done>
Navigation updated:
- Cari Hesap link added
- Wallet icon
- Proper active state
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `npm run build` passes
2. Financials page accessible at /financials
3. Balance shows correct values
4. Transactions display with proper formatting
5. Date filters work correctly
6. Type filter works correctly
7. Invoice download generates signed URL
8. Navigation link present and working
</verification>

<success_criteria>
- FIN-01: Bayi cari hesap bakiyesini gorur (toplam borc, alacak, net bakiye) ✓
- FIN-02: Bayi cari hesap hareketlerini listeler (fatura, odeme, duzeltme) ✓
- FIN-03: Bayi faturalarini listeler ve PDF olarak indirir ✓
- FIN-04: Bayi odeme gecmisini gorur (tarih, tutar, yontem) ✓
</success_criteria>

<output>
After completion, create `.planning/phases/05-financial-backbone/05-02-SUMMARY.md`
</output>
