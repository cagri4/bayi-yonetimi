---
phase: 06-dashboard-campaigns-docs
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - src/lib/actions/catalog.ts
  - src/app/(dealer)/catalog/page.tsx
  - src/components/layout/nav-links.tsx
autonomous: false

must_haves:
  truths:
    - "Dealer can filter catalog by 'yeni urunler' tag"
    - "New products filter shows products added in last 30 days"
    - "All Phase 6 navigation links are present"
    - "Dashboard is accessible and functional"
    - "Campaigns page works end-to-end"
    - "Order documents work end-to-end"
  artifacts:
    - path: "src/app/(dealer)/catalog/page.tsx"
      provides: "Catalog with new products filter"
      contains: "yeni"
  key_links:
    - from: "Catalog filter"
      to: "products.created_at"
      via: "Date comparison query"
      pattern: "created_at.*interval"
---

<objective>
Add new products filter to catalog and verify all Phase 6 features work together.

Purpose: Complete CAMP-04 requirement and ensure integrated functionality across dashboard, campaigns, and order documents.
Output: New products catalog filter and verified Phase 6 functionality.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dashboard-campaigns-docs/06-RESEARCH.md
@.planning/phases/06-dashboard-campaigns-docs/06-02-SUMMARY.md
@.planning/phases/06-dashboard-campaigns-docs/06-03-SUMMARY.md
@.planning/phases/06-dashboard-campaigns-docs/06-04-SUMMARY.md
@src/lib/actions/catalog.ts
@src/app/(dealer)/catalog/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new products filter to catalog</name>
  <files>
    src/lib/actions/catalog.ts
    src/app/(dealer)/catalog/page.tsx
  </files>
  <action>
1. **Update src/lib/actions/catalog.ts**:
   - Modify getCatalogProducts to accept optional `isNew?: boolean` filter
   - When isNew is true:
     - Add WHERE condition: created_at >= NOW() - INTERVAL '30 days'
     - OR add is_new column check if that exists
   - Ensure index on products.created_at exists (add to migration if needed)
   - Keep existing filters (category, search, favorites) working

2. **Update src/app/(dealer)/catalog/page.tsx**:
   - Add "Yeni Urunler" filter option to existing filter UI
   - Options: Tumu | Yeni Urunler | Favoriler (extend current)
   - Use URL search params for filter state (server-side filtering)
   - When "Yeni Urunler" selected:
     - Pass isNew: true to getCatalogProducts
     - Show "Yeni" badge on filtered products
   - Badge styling: Small green/blue tag on product cards

3. **Visual indicator on product cards**:
   - If product.created_at is within 30 days, show "Yeni" badge
   - Badge position: Top-right corner of product card image
   - Style: bg-green-500 text-white text-xs px-2 py-1 rounded

Filter should be toggleable independent of other filters (can combine with favorites).
  </action>
  <verify>
    - /catalog?filter=new shows only products from last 30 days
    - New products have "Yeni" badge on cards
    - Filter combines with other filters (category, search)
    - npm run build passes
  </verify>
  <done>New products filter added to catalog with visual badge</done>
</task>

<task type="auto">
  <name>Task 2: Ensure all navigation links and install @react-pdf/renderer</name>
  <files>
    src/components/layout/nav-links.tsx
    package.json
  </files>
  <action>
1. **Verify and complete nav-links.tsx**:
   Check that ALL Phase 6 navigation links are present:

   Dealer navigation (in order):
   - Dashboard (LayoutDashboard) -> /dashboard
   - Katalog (Package) -> /catalog
   - Favorilerim (Heart) -> /favorites
   - Cari Hesap (Wallet) -> /financials
   - Kampanyalar (Megaphone) -> /campaigns
   - Duyurular (Bell) -> /announcements
   - Siparislerim (ShoppingBag) -> /orders
   - Sepetim (ShoppingCart) -> /cart

   Admin navigation (in order):
   - Dashboard -> /admin
   - Urunler -> /admin/products
   - Bayiler -> /admin/dealers
   - Siparisler -> /admin/orders
   - Kampanyalar -> /admin/campaigns
   - Duyurular -> /admin/announcements
   - Raporlar -> /admin/reports

   Add any missing links. Ensure consistent ordering.

2. **Install @react-pdf/renderer** (for future PDF generation):
   - Run: npm install @react-pdf/renderer
   - This enables server-side invoice PDF generation
   - Note: Actual PDF template creation deferred, but dependency installed

3. **Verify unread announcement badge**:
   - Duyurular link should show unread count badge
   - Fetch count via getUnreadAnnouncementCount()
   - Display as small red circle with number
  </action>
  <verify>
    - All navigation links present and working
    - @react-pdf/renderer in package.json dependencies
    - npm install completes without errors
    - npm run build passes
  </verify>
  <done>Navigation complete and PDF dependency installed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 6 end-to-end functionality</name>
  <what-built>
    Complete Phase 6: Dashboard, Campaigns, Announcements, Order Documents, New Products Filter
  </what-built>
  <how-to-verify>
    1. **Dashboard verification:**
       - Login as dealer
       - Navigate to /dashboard
       - Verify: Spending summary shows (may be empty if no transactions)
       - Verify: Recent orders widget shows last orders
       - Verify: Pending count displays
       - Verify: Quick actions buttons work (navigate to correct pages)
       - Verify: Top products shows (may be empty if no order history)

    2. **Campaigns verification:**
       - Navigate to /campaigns
       - Verify: Campaign list page loads (may be empty)
       - Login as admin, go to /admin/campaigns
       - Create a test campaign with products
       - Return to dealer view, verify campaign appears
       - Click campaign, verify detail page with products

    3. **Announcements verification:**
       - Navigate to /announcements as dealer
       - Login as admin, go to /admin/announcements
       - Create a test announcement
       - Return to dealer view, verify announcement appears as unread
       - Click announcement, verify it marks as read (visual change)
       - Verify badge count updates

    4. **Order documents verification:**
       - Login as admin, go to /admin/orders/{order-id}
       - Upload a test PDF as "Fatura"
       - Enter cargo info (plate, driver name)
       - Login as dealer, go to /orders/{same-order-id}
       - Verify: Document download button appears
       - Verify: Cargo info section shows entered data
       - Click download, verify PDF opens/downloads

    5. **New products filter:**
       - Navigate to /catalog
       - Apply "Yeni Urunler" filter
       - Verify: Only recent products shown (or empty if none)
       - Verify: "Yeni" badge on product cards
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. All Phase 6 pages accessible and functional
2. Navigation complete with all links
3. Data flows correctly: admin creates -> dealer sees
4. Documents upload and download work
5. Build passes: `npm run build`
</verification>

<success_criteria>
- DASH-01 through DASH-06: Dashboard fully functional
- CAMP-01 through CAMP-06: Campaigns and announcements working
- CAMP-04: "Yeni urunler" filter in catalog
- ORD-01 through ORD-05: Order documents and cargo tracking complete
- All navigation links present
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-campaigns-docs/06-05-SUMMARY.md`
</output>
