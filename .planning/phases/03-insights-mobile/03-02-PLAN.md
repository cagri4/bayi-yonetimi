---
phase: 03-insights-mobile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/003_reporting_functions.sql
  - src/lib/queries/reports.ts
  - src/app/(admin)/admin/reports/products/page.tsx
  - src/app/(admin)/admin/reports/dealers/page.tsx
  - src/components/reports/top-products-table.tsx
  - src/components/reports/dealer-performance-table.tsx
  - src/lib/actions/export-reports.ts
autonomous: true

must_haves:
  truths:
    - "Admin can view top selling products by quantity and revenue"
    - "Admin can view dealer performance with sales ranking"
    - "Admin can export any report as CSV file"
    - "CSV export includes Turkish column headers"
  artifacts:
    - path: "supabase/migrations/003_reporting_functions.sql"
      provides: "get_top_products and get_dealer_performance RPC functions"
      contains: "RANK() OVER"
    - path: "src/app/(admin)/admin/reports/products/page.tsx"
      provides: "Top products report page"
      exports: ["default"]
    - path: "src/app/(admin)/admin/reports/dealers/page.tsx"
      provides: "Dealer performance page"
      exports: ["default"]
    - path: "src/lib/actions/export-reports.ts"
      provides: "CSV export server actions"
      contains: "csv-stringify"
  key_links:
    - from: "src/app/(admin)/admin/reports/products/page.tsx"
      to: "src/lib/queries/reports.ts"
      via: "getTopProducts function"
      pattern: "getTopProducts"
    - from: "src/lib/actions/export-reports.ts"
      to: "csv-stringify"
      via: "streaming CSV generation"
      pattern: "stringify"
---

<objective>
Create top products report, dealer performance report, and CSV export functionality.

Purpose: Admin needs to identify best-selling products and top-performing dealers for inventory and relationship management decisions. CSV export enables offline analysis.
Output: Two report pages and CSV export capability for all reports
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-insights-mobile/03-RESEARCH.md

# Existing patterns
@src/lib/supabase/server.ts
@src/lib/actions/admin-orders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Top Products and Dealer Performance Database Functions</name>
  <files>
    supabase/migrations/003_reporting_functions.sql
    src/lib/queries/reports.ts
  </files>
  <action>
1. Update migration file `supabase/migrations/003_reporting_functions.sql` (append to existing):

   Add `get_top_products(start_date date, end_date date, limit_count int DEFAULT 10)` function:
   - JOIN products with order_items and orders
   - Filter by date range and exclude cancelled orders
   - Return: product_id, product_name, sku, order_count, total_quantity, total_revenue
   - ORDER BY total_quantity DESC
   - LIMIT by limit_count parameter

   Add `get_dealer_performance(start_date date, end_date date)` function:
   - JOIN dealers with orders
   - Filter by date range and exclude cancelled orders
   - Return: dealer_id, company_name, order_count, total_sales, avg_order_value, sales_rank, sales_percentage
   - Use RANK() OVER (ORDER BY SUM(total_amount) DESC) for ranking
   - Calculate percentage: SUM(total_amount) * 100.0 / SUM(SUM(total_amount)) OVER ()
   - ORDER BY total_sales DESC NULLS LAST

2. Update `src/lib/queries/reports.ts`:

   Add `getTopProducts(startDate: string, endDate: string, limit?: number)`:
   - Call supabase.rpc('get_top_products', { start_date, end_date, limit_count })
   - Return typed array with product metrics

   Add `getDealerPerformance(startDate: string, endDate: string)`:
   - Call supabase.rpc('get_dealer_performance', { start_date, end_date })
   - Return typed array with dealer metrics including rank

Export types for components:
- TopProductRow: { product_id, product_name, sku, order_count, total_quantity, total_revenue }
- DealerPerformanceRow: { dealer_id, company_name, order_count, total_sales, avg_order_value, sales_rank, sales_percentage }
  </action>
  <verify>
    - Migration file contains both new functions
    - src/lib/queries/reports.ts exports getTopProducts and getDealerPerformance
    - Run `npm run build` - no TypeScript errors
  </verify>
  <done>
    - get_top_products function aggregates product sales with quantity and revenue
    - get_dealer_performance function includes ranking and percentage
    - Query functions typed and exported
  </done>
</task>

<task type="auto">
  <name>Task 2: Report Pages and CSV Export</name>
  <files>
    src/app/(admin)/admin/reports/products/page.tsx
    src/app/(admin)/admin/reports/dealers/page.tsx
    src/components/reports/top-products-table.tsx
    src/components/reports/dealer-performance-table.tsx
    src/lib/actions/export-reports.ts
    src/app/(admin)/admin/reports/page.tsx
  </files>
  <action>
1. Install csv-stringify:
   ```bash
   pnpm add csv-stringify
   ```

2. Create `src/lib/actions/export-reports.ts`:
   - "use server" directive
   - Import { stringify } from 'csv-stringify/sync' (sync for simplicity, data is pre-aggregated)

   Export `exportSalesReportCSV(periodType, startDate, endDate)`:
   - Fetch data using getSalesReport
   - Use stringify with Turkish column headers: { Donem, Siparis_Sayisi, Toplam_Satis, Ortalama_Siparis }
   - Return CSV string

   Export `exportTopProductsCSV(startDate, endDate)`:
   - Fetch data using getTopProducts (no limit for export)
   - Headers: { Urun_Adi, SKU, Siparis_Sayisi, Toplam_Adet, Toplam_Ciro }
   - Return CSV string

   Export `exportDealerPerformanceCSV(startDate, endDate)`:
   - Fetch data using getDealerPerformance
   - Headers: { Bayi_Adi, Siparis_Sayisi, Toplam_Satis, Ortalama_Siparis, Siralama, Yuzde }
   - Return CSV string

3. Create `src/components/reports/top-products-table.tsx`:
   - Server component (no interactivity needed)
   - Props: data array of TopProductRow
   - shadcn Table with columns: Sira, Urun Adi, SKU, Siparis Sayisi, Satis Adedi, Toplam Ciro
   - Format currency with toLocaleString('tr-TR')
   - Empty state if no data

4. Create `src/components/reports/dealer-performance-table.tsx`:
   - Server component
   - Props: data array of DealerPerformanceRow
   - shadcn Table with columns: Sira, Bayi Adi, Siparis Sayisi, Toplam Satis, Ort. Siparis, Pay (%)
   - Show rank with medal emoji for top 3 (gold, silver, bronze)
   - Format percentages with 1 decimal place

5. Create `src/app/(admin)/admin/reports/products/page.tsx`:
   - Server component
   - Read searchParams for startDate (default 30 days), endDate (default today)
   - Fetch data with getTopProducts
   - Date range picker (reuse pattern from sales page)
   - "CSV Indir" button that triggers exportTopProductsCSV
   - Render TopProductsTable

6. Create `src/app/(admin)/admin/reports/dealers/page.tsx`:
   - Server component
   - Same date range pattern
   - "CSV Indir" button for dealer export
   - Render DealerPerformanceTable

7. Update `src/app/(admin)/admin/reports/page.tsx`:
   - Reports index with cards linking to:
     - /admin/reports/sales (Satis Raporu)
     - /admin/reports/products (En Cok Satan Urunler)
     - /admin/reports/dealers (Bayi Performansi)
   - Each card shows icon and brief description

CSV Download Pattern (client-side trigger):
```tsx
// In page component
async function handleExport() {
  const csv = await exportTopProductsCSV(startDate, endDate)
  const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = `urunler-${startDate}-${endDate}.csv`
  a.click()
}
```
Note: Export button needs to be in a client component wrapper for the download trigger.
  </action>
  <verify>
    - Run `npm run build` - no errors
    - Navigate to /admin/reports - shows three report cards
    - Navigate to /admin/reports/products - table renders
    - Navigate to /admin/reports/dealers - table with ranks renders
    - CSV export buttons present on each report page
  </verify>
  <done>
    - Top products page shows ranked products by sales quantity
    - Dealer performance page shows dealers with ranking and percentage
    - All three reports have CSV export capability
    - Reports index page links to all report types
  </done>
</task>

</tasks>

<verification>
1. Database functions complete:
   - get_top_products aggregates by quantity and revenue
   - get_dealer_performance includes ranking window function

2. Report pages functional:
   - Top products shows products sorted by quantity sold
   - Dealer performance shows ranked dealers with percentage

3. CSV export works:
   - Each report has export button
   - CSV files have Turkish headers
   - Download triggers in browser
</verification>

<success_criteria>
- Admin can view /admin/reports/products with top selling products
- Admin can view /admin/reports/dealers with dealer rankings
- Admin can export any report as CSV with Turkish headers
- Tables show formatted currency and percentages
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-insights-mobile/03-02-SUMMARY.md`
</output>
