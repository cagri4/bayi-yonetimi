---
phase: 03-insights-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/003_reporting_functions.sql
  - src/components/ui/chart.tsx
  - src/lib/queries/reports.ts
  - src/app/(admin)/admin/reports/page.tsx
  - src/app/(admin)/admin/reports/sales/page.tsx
  - src/components/reports/sales-chart.tsx
  - src/components/reports/period-selector.tsx
  - src/app/(admin)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can view daily sales report with chart visualization"
    - "Admin can switch between daily/weekly/monthly aggregation periods"
    - "Admin can select custom date range for sales report"
    - "Sales data shows order count and total revenue per period"
  artifacts:
    - path: "supabase/migrations/003_reporting_functions.sql"
      provides: "get_sales_report RPC function"
      contains: "DATE_TRUNC"
    - path: "src/components/reports/sales-chart.tsx"
      provides: "Bar chart visualization"
      contains: "use client"
    - path: "src/app/(admin)/admin/reports/sales/page.tsx"
      provides: "Sales report page"
      exports: ["default"]
  key_links:
    - from: "src/app/(admin)/admin/reports/sales/page.tsx"
      to: "src/lib/queries/reports.ts"
      via: "server-side data fetch"
      pattern: "getSalesReport"
    - from: "src/components/reports/sales-chart.tsx"
      to: "recharts"
      via: "BarChart component"
      pattern: "BarChart.*data"
---

<objective>
Create admin sales reporting dashboard with time-series charts.

Purpose: Admin needs to view sales performance over time (daily/weekly/monthly) to make business decisions. This is the first reporting feature for Phase 3.
Output: Database reporting function, chart components, and sales report page accessible at /admin/reports/sales
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-insights-mobile/03-RESEARCH.md

# Existing patterns
@src/lib/supabase/server.ts
@src/app/(admin)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Reporting Function and shadcn Chart Setup</name>
  <files>
    supabase/migrations/003_reporting_functions.sql
    src/components/ui/chart.tsx
    src/lib/queries/reports.ts
  </files>
  <action>
1. Create migration file `supabase/migrations/003_reporting_functions.sql`:
   - Create `get_sales_report(period_type text, start_date date, end_date date)` function
   - Uses DATE_TRUNC for day/week/month aggregation
   - Returns: period (timestamp), order_count (bigint), total_sales (numeric), avg_order_value (numeric)
   - Filters out cancelled orders (status != 'cancelled')
   - ORDER BY period DESC

2. Install shadcn chart component:
   ```bash
   pnpm dlx shadcn@latest add chart
   ```
   This creates src/components/ui/chart.tsx with ChartContainer, ChartTooltip, ChartTooltipContent.

3. Create `src/lib/queries/reports.ts`:
   - Export `getSalesReport(periodType: 'day' | 'week' | 'month', startDate: string, endDate: string)` function
   - Uses createClient from @/lib/supabase/server
   - Calls supabase.rpc('get_sales_report', { period_type, start_date, end_date })
   - Returns typed array: { period: string, order_count: number, total_sales: number, avg_order_value: number }[]
   - Handle errors with try/catch, return empty array on error

Note: Use existing Supabase server client pattern from src/lib/supabase/server.ts
  </action>
  <verify>
    - File exists: supabase/migrations/003_reporting_functions.sql
    - File exists: src/components/ui/chart.tsx
    - File exists: src/lib/queries/reports.ts
    - Run `npm run build` - no TypeScript errors
  </verify>
  <done>
    - Migration file contains get_sales_report function with DATE_TRUNC
    - Chart components installed from shadcn
    - Reports query module exports getSalesReport function
  </done>
</task>

<task type="auto">
  <name>Task 2: Sales Report Page with Chart and Period Selector</name>
  <files>
    src/app/(admin)/admin/reports/page.tsx
    src/app/(admin)/admin/reports/sales/page.tsx
    src/components/reports/sales-chart.tsx
    src/components/reports/period-selector.tsx
    src/app/(admin)/layout.tsx
  </files>
  <action>
1. Create `src/components/reports/period-selector.tsx`:
   - "use client" directive (requires interactivity)
   - Props: periodType, startDate, endDate, onPeriodChange, onDateRangeChange
   - Select dropdown for period type (Gunluk/Haftalik/Aylik)
   - Two date inputs for start/end date
   - Use URL search params for state (existing pattern from admin orders)
   - Use shadcn Select and Input components

2. Create `src/components/reports/sales-chart.tsx`:
   - "use client" directive (Recharts requires browser APIs)
   - Props: data array with period, order_count, total_sales
   - Use shadcn ChartContainer with chartConfig
   - BarChart from recharts with:
     - CartesianGrid (vertical={false})
     - XAxis with Turkish date formatting (date-fns format)
     - ChartTooltip with ChartTooltipContent
     - Two bars: one for order_count, one for total_sales (different colors)
   - Handle empty data with centered "Veri bulunamadi" message

3. Create `src/app/(admin)/admin/reports/page.tsx`:
   - Simple redirect or index page linking to /admin/reports/sales
   - Can be a server component with just navigation links

4. Create `src/app/(admin)/admin/reports/sales/page.tsx`:
   - Server component for data fetching
   - Read searchParams for periodType (default 'day'), startDate (default 30 days ago), endDate (default today)
   - Call getSalesReport with params
   - Render page title "Satis Raporu"
   - Render PeriodSelector and SalesChart
   - Show summary cards: total orders, total revenue, average order value for the period

5. Update `src/app/(admin)/layout.tsx`:
   - Add "Raporlar" link to admin nav pointing to /admin/reports
   - Use BarChart3 icon from lucide-react

Turkish labels:
- "Donem": Period
- "Gunluk": Daily
- "Haftalik": Weekly
- "Aylik": Monthly
- "Baslangic": Start date
- "Bitis": End date
- "Toplam Siparis": Total Orders
- "Toplam Ciro": Total Revenue
- "Ortalama Siparis": Average Order Value
  </action>
  <verify>
    - Run `npm run build` - no errors
    - Run `npm run dev`, navigate to /admin/reports/sales
    - Page loads without errors (may show empty data if migration not applied)
    - Period selector renders with options
    - Chart component renders (empty state if no data)
  </verify>
  <done>
    - Admin can access /admin/reports/sales from navigation
    - Page shows period selector with day/week/month options
    - Page shows date range picker
    - Chart renders sales data (or empty state)
    - Summary cards show totals for selected period
  </done>
</task>

</tasks>

<verification>
1. Database function ready for deployment:
   - Migration file exists with proper SQL syntax
   - Function handles all three period types (day, week, month)

2. UI components functional:
   - Chart component uses "use client" directive
   - Period selector updates URL params
   - Date range picker works

3. Integration complete:
   - Navigation link added to admin layout
   - Data flows from DB -> query -> page -> chart
</verification>

<success_criteria>
- Admin can navigate to /admin/reports/sales
- Admin can select period type (daily/weekly/monthly)
- Admin can select date range
- Chart displays sales data with bars for order count and revenue
- Summary cards show totals
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-insights-mobile/03-01-SUMMARY.md`
</output>
