---
phase: 16-kurulum-sihirbazi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sihirbaz/agents.ts
  - src/lib/sihirbaz/session.ts
  - src/lib/sihirbaz/steps.ts
autonomous: true
requirements: [KS-02, KS-03, KS-04, KS-07]

must_haves:
  truths:
    - "Token validation rejects used or expired invite tokens with Turkish error message"
    - "Session is created on valid /start and resumed on subsequent messages via telegram_chat_id lookup"
    - "Wizard collects firma_adi, sektor, urun_sayisi, bayi_sayisi, beklentiler across steps 1-5"
    - "All 12 dijital calisanlar are introduced by name with Turkish descriptions before confirmation"
    - "User who confirms receives a fresh temp password and web panel URL"
    - "User who disconnects mid-flow can resume from the same step"
  artifacts:
    - path: "src/lib/sihirbaz/agents.ts"
      provides: "12 agent descriptions array with role, name, description"
      contains: "AGENT_DESCRIPTIONS"
    - path: "src/lib/sihirbaz/session.ts"
      provides: "Session load/create/update helpers for onboarding_sessions table"
      exports: ["loadOrCreateSession", "updateSession", "WizardSession"]
    - path: "src/lib/sihirbaz/steps.ts"
      provides: "Step handler dispatch for all wizard steps 0-7 plus completed state"
      exports: ["handleStep"]
  key_links:
    - from: "src/lib/sihirbaz/session.ts"
      to: "onboarding_invites table"
      via: "SHA-256 token hash lookup on /start"
      pattern: "crypto\\.subtle\\.digest.*SHA-256"
    - from: "src/lib/sihirbaz/steps.ts"
      to: "src/lib/sihirbaz/session.ts"
      via: "updateSession calls to advance step + save collected_data"
      pattern: "updateSession.*step.*collected_data"
    - from: "src/lib/sihirbaz/steps.ts"
      to: "src/lib/sihirbaz/agents.ts"
      via: "AGENT_DESCRIPTIONS import for step 6 intro message"
      pattern: "AGENT_DESCRIPTIONS"
    - from: "src/lib/sihirbaz/steps.ts"
      to: "auth.admin.updateUserById"
      via: "Fresh temp password generation at provisioning completion"
      pattern: "auth\\.admin\\.updateUserById"
---

<objective>
Create the WizardOrchestrator FSM core — session management, step handler functions, and agent descriptions data file. These three library modules implement the full wizard conversation logic: token validation, 5-step data collection, 12-agent introduction, confirmation, and provisioning completion with temp password reset.

Purpose: The wizard is a step-based state machine that reads/writes to onboarding_sessions. This plan creates all the business logic; Plan 02 wires it into the webhook route.
Output: src/lib/sihirbaz/agents.ts, session.ts, steps.ts — ready to be called by the dispatcher in Plan 02.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/15-company-creation-infrastructure/15-01-SUMMARY.md
@.planning/phases/16-kurulum-sihirbazi/16-RESEARCH.md

@src/lib/actions/superadmin.ts
@src/lib/telegram/send.ts
@src/lib/supabase/service-client.ts
@src/app/api/telegram/sihirbaz/route.ts
@supabase/migrations/012_v4_schema_foundation.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent descriptions data file and session management module</name>
  <files>src/lib/sihirbaz/agents.ts, src/lib/sihirbaz/session.ts</files>
  <action>
Create two new files in src/lib/sihirbaz/:

**agents.ts:**
- Export `AGENT_DESCRIPTIONS` — an array of 12 objects, each with `role` (matching AgentRole enum values), `name` (Turkish display name), and `description` (one-sentence Turkish description of what the agent does).
- The 12 roles in order: egitimci, satis_temsilcisi, muhasebeci, depo_sorumlusu, genel_mudur_danismani, tahsilat_uzmani, dagitim_koordinatoru, saha_satis, pazarlamaci, urun_yoneticisi, satin_alma, iade_kalite.
- Use the descriptions from the research file (src/lib/sihirbaz section of 16-RESEARCH.md) as the baseline. These are business-appropriate Turkish descriptions.

**session.ts:**
- Export type `WizardSession` matching the onboarding_sessions row shape: `{ id: string, company_id: string | null, deep_link_token: string, telegram_chat_id: number | null, status: 'pending' | 'in_progress' | 'completed' | 'expired', collected_data: Record<string, unknown>, step: number }`.
- Export `loadOrCreateSession(supabase: SupabaseClient, chatId: number, text: string): Promise<WizardSession | null>`.
  - Case 1 — `/start {token}`: Extract raw token from text. SHA-256 hash it via `crypto.subtle.digest('SHA-256', new TextEncoder().encode(rawToken))` and convert to hex string. First check for existing session by `deep_link_token = rawToken` (resume case). If found and status is not 'expired', return it (update telegram_chat_id if null). If no existing session, query `onboarding_invites` WHERE `token_hash = hash AND used_at IS NULL AND expires_at > NOW()`. If invalid/used/expired, return null. Otherwise create new session (company_id from invite, deep_link_token = rawToken, telegram_chat_id = chatId, status = 'in_progress', collected_data = {}, step = 0) and mark invite `used_at = NOW()`.
  - Case 2 — any other message: Query `onboarding_sessions` WHERE `telegram_chat_id = chatId AND status IN ('pending', 'in_progress')`, order by created_at DESC, limit 1, maybeSingle. Return result or null.
  - Use `(supabase as any)` cast for onboarding_invites and onboarding_sessions queries (consistent with Phase 15 pattern — tables are in DB but typed client may not expose them fully).
- Export `updateSession(supabase: SupabaseClient, sessionId: string, patch: Partial<Pick<WizardSession, 'step' | 'collected_data' | 'status' | 'company_id' | 'telegram_chat_id'>>): Promise<void>`.
  - Simple UPDATE via supabase.from('onboarding_sessions').update(patch).eq('id', sessionId).

IMPORTANT: The session lookup order in /start case MUST check for existing session BEFORE checking invite used_at. This allows a user who clicked /start but didn't finish to click /start again and resume — the invite is already marked used_at but the existing session is still in_progress.
  </action>
  <verify>
Run `npx tsc --noEmit src/lib/sihirbaz/agents.ts src/lib/sihirbaz/session.ts` — no TypeScript errors. Verify AGENT_DESCRIPTIONS has exactly 12 entries. Verify loadOrCreateSession handles both /start and non-/start cases.
  </verify>
  <done>
agents.ts exports AGENT_DESCRIPTIONS with 12 entries matching all AgentRole values. session.ts exports WizardSession type, loadOrCreateSession (with token validation + resume), and updateSession. SHA-256 hash logic matches Phase 15's generateInviteTokenAndLink pattern exactly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create step handler functions with provisioning completion</name>
  <files>src/lib/sihirbaz/steps.ts</files>
  <action>
Create src/lib/sihirbaz/steps.ts:

Export `handleStep(supabase: SupabaseClient, session: WizardSession, chatId: number, text: string, botToken: string): Promise<void>`.

Implement a switch on `session.step`:

**Step 0 (Welcome):** Send greeting message in Turkish: "Hosgeldiniz! Ben Kurulum Sihirbazinim. Firmanizi dijital donusume hazirlamak icin size yardimci olacagim.\n\nOnce birkac bilgiye ihtiyacim var.\n\nFirmanizin adi nedir?" Advance to step 1 via updateSession.

**Step 1 (Firma adi):** Save text as collected_data.firma_adi. Send "Hangi sektorde faaliyet gosteriyorsunuz? (Ornek: Gida, Tekstil, Elektronik, Insaat)". Advance to step 2.

**Step 2 (Sektor):** Save text as collected_data.sektor. Send "Yaklasik kac cesit urun satiyorsunuz?". Advance to step 3.

**Step 3 (Urun sayisi):** Save text as collected_data.urun_sayisi. Send "Kac bayiniz var? (Yaklasik sayi)". Advance to step 4.

**Step 4 (Bayi sayisi):** Save text as collected_data.bayi_sayisi. Send "Dijital asistanlarinizdan en cok ne bekliyorsunuz? (Siparis takibi, stok yonetimi, muhasebe vb.)". Advance to step 5.

**Step 5 (Beklentiler):** Save text as collected_data.beklentiler. Now send the 12 agent introductions. Build intro text from AGENT_DESCRIPTIONS: each entry formatted as "${i+1}. ${name}\n${description}" joined by double newlines. Prefix with "Harika! Simdi size 12 dijital calisaninizi tanistirmak istiyorum:\n\n". Check combined message length — if > 3800 chars, split into two messages (first 6 + last 6). After intro messages, send confirmation prompt: "Tum bu dijital calisanlar 14 gun boyunca ucretsiz olarak hizmetinizde olacak.\n\nKurulumu baslatmak istiyor musunuz? (Evet / Hayir)". Advance to step 6.

**Step 6 (Confirmation):** Check if text contains "evet" (case-insensitive) or "e" or "1". If confirmed: call `runProvisioning(supabase, session, chatId, botToken)`. If rejected (hayir/h/0): send "Anlasildı. Kurulumu iptal ettiniz. Yeni bir davet linki icin yetkilinize basvurun." and update session status to 'expired'.

**Step 7+ / completed status (Post-completion):** Send "Kurulum zaten tamamlandi. Web panelinize giris yapabilirsiniz." — no state change.

**runProvisioning helper (private function in same file):**
1. Update company settings with collected data: `supabase.from('companies').update({ settings: { sektor, urun_sayisi, bayi_sayisi, beklentiler, onboarding_completed_at: new Date().toISOString() } }).eq('id', session.company_id)`.
2. Generate fresh temp password: `crypto.randomUUID().slice(0, 12)`.
3. Look up company admin user: `supabase.from('users').select('id').eq('company_id', session.company_id).eq('role', 'admin').single()`.
4. Reset admin password: `supabase.auth.admin.updateUserById(adminUser.id, { password: newTempPassword })`.
5. Update session: status='completed', step=7.
6. Send completion message: "Kurulum tamamlandi!\n\nWeb paneliniz: {NEXT_PUBLIC_APP_URL || 'https://bayi-yonetimi.vercel.app'}\n\nGiris bilgileriniz:\nE-posta: {admin email from users table}\nGecici sifre: {newTempPassword}\n\nIlk giriste sifrenizi degistirmenizi oneririz.\n\n14 gunluk ucretsiz deneme suresi baslamistir. Keyifli kullanımlar!"
7. If company_id is null (should not happen if invite validation worked), send error message and return early.

IMPORTANT: Use `(supabase as any)` for companies.update and users.select — consistent with Phase 15 pattern.
IMPORTANT: Steps 0-5 use `sendTelegramMessage` from `@/lib/telegram/send` for all bot replies.
IMPORTANT: For step 5 (agent intros), after sending the intro message, do NOT send a separate confirmation message in a different step — send both the intros and the confirmation prompt in step 5, then advance directly to step 6 (the confirmation handler). This keeps the FSM simpler: step 5 = receive beklentiler + send intros + send confirmation, step 6 = receive evet/hayir.
  </action>
  <verify>
Run `npx tsc --noEmit src/lib/sihirbaz/steps.ts` — no TypeScript errors. Verify all 8 step cases are handled (0-6 + default/completed). Verify runProvisioning generates a new temp password via auth.admin.updateUserById. Verify AGENT_DESCRIPTIONS is imported from ./agents. Verify sendTelegramMessage is imported from @/lib/telegram/send.
  </verify>
  <done>
steps.ts exports handleStep covering the complete wizard flow: welcome (step 0), 5-step data collection (steps 1-5), agent introduction + confirmation prompt (step 5 output), confirmation handler (step 6), and post-completion guard. runProvisioning generates a fresh temp password, updates company settings, and sends completion message with web panel URL and credentials.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all three files in src/lib/sihirbaz/
2. AGENT_DESCRIPTIONS has exactly 12 entries
3. loadOrCreateSession SHA-256 hash pattern matches Phase 15 generateInviteTokenAndLink
4. All step transitions are sequential (0->1->2->3->4->5->6->completed)
5. runProvisioning uses auth.admin.updateUserById for fresh password (not stored password)
6. collected_data merges correctly across steps (spread pattern)
</verification>

<success_criteria>
- Three library files exist in src/lib/sihirbaz/ and compile without errors
- Session management handles both /start (token validation) and message-resume paths
- Step handlers cover the complete wizard conversation flow with Turkish messages
- Provisioning completion generates a new temp password and sends credentials via Telegram
</success_criteria>

<output>
After completion, create `.planning/phases/16-kurulum-sihirbazi/16-01-SUMMARY.md`
</output>
