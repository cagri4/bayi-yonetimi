---
phase: 12-extended-agent-ecosystem
plan: 06
type: execute
wave: 2
depends_on:
  - 12-02
  - 12-03
  - 12-04
files_modified:
  - src/app/api/telegram/tahsilat-uzmani/route.ts
  - src/app/api/telegram/dagitim-koordinatoru/route.ts
  - src/app/api/telegram/saha-satis/route.ts
  - src/app/api/telegram/pazarlamaci/route.ts
  - src/app/api/telegram/urun-yoneticisi/route.ts
  - src/app/api/telegram/satin-alma/route.ts
  - src/app/api/telegram/iade-kalite/route.ts
  - SS/12-agent-definitions-seed.sql
autonomous: false
requirements:
  - AO-01

must_haves:
  truths:
    - "7 new webhook route files exist at /api/telegram/{role}/route.ts"
    - "Each route uses the correct env var: TELEGRAM_BOT_TOKEN_{ROLE_UPPERCASE}"
    - "Each route passes the correct role string to dispatchAgentUpdate"
    - "All 7 new routes are exact copies of egitimci/route.ts with only role and env var substitutions"
    - "SS/12-agent-definitions-seed.sql contains INSERT statements for all 7 new agent roles"
    - "Seed SQL follows the same DO $$ pattern as SS/11-agent-definitions-seed.sql"
  artifacts:
    - path: "src/app/api/telegram/tahsilat-uzmani/route.ts"
      provides: "Webhook route for tahsilat_uzmani agent"
      contains: "TELEGRAM_BOT_TOKEN_TAHSILAT_UZMANI"
    - path: "src/app/api/telegram/dagitim-koordinatoru/route.ts"
      provides: "Webhook route for dagitim_koordinatoru agent"
      contains: "TELEGRAM_BOT_TOKEN_DAGITIM_KOORDINATORU"
    - path: "src/app/api/telegram/saha-satis/route.ts"
      provides: "Webhook route for saha_satis agent"
      contains: "TELEGRAM_BOT_TOKEN_SAHA_SATIS"
    - path: "src/app/api/telegram/pazarlamaci/route.ts"
      provides: "Webhook route for pazarlamaci agent"
      contains: "TELEGRAM_BOT_TOKEN_PAZARLAMACI"
    - path: "src/app/api/telegram/urun-yoneticisi/route.ts"
      provides: "Webhook route for urun_yoneticisi agent"
      contains: "TELEGRAM_BOT_TOKEN_URUN_YONETICISI"
    - path: "src/app/api/telegram/satin-alma/route.ts"
      provides: "Webhook route for satin_alma agent"
      contains: "TELEGRAM_BOT_TOKEN_SATIN_ALMA"
    - path: "src/app/api/telegram/iade-kalite/route.ts"
      provides: "Webhook route for iade_kalite agent"
      contains: "TELEGRAM_BOT_TOKEN_IADE_KALITE"
    - path: "SS/12-agent-definitions-seed.sql"
      provides: "SQL seed for 7 new agent definitions"
      contains: "tahsilat_uzmani"
  key_links:
    - from: "tahsilat-uzmani/route.ts"
      to: "dispatchAgentUpdate"
      via: "dispatchAgentUpdate(update, 'tahsilat_uzmani', botToken)"
      pattern: "tahsilat_uzmani"
    - from: "iade-kalite/route.ts"
      to: "dispatchAgentUpdate"
      via: "dispatchAgentUpdate(update, 'iade_kalite', botToken)"
      pattern: "iade_kalite"
---

<objective>
Create 7 new Telegram webhook routes for the Phase 12 agents and the SQL seed file for their agent_definitions entries.

Purpose: Each agent needs a dedicated webhook URL (for Telegram bot registration) and a database row in agent_definitions (for system prompt and model). The routes are identical to egitimci/route.ts with substituted role names and env vars. This plan can run in parallel with plan 05 since it touches different files.

Output: 7 new route files, SS/12-agent-definitions-seed.sql.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/app/api/telegram/egitimci/route.ts
@SS/11-agent-definitions-seed.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 Telegram webhook route files</name>
  <files>
    src/app/api/telegram/tahsilat-uzmani/route.ts
    src/app/api/telegram/dagitim-koordinatoru/route.ts
    src/app/api/telegram/saha-satis/route.ts
    src/app/api/telegram/pazarlamaci/route.ts
    src/app/api/telegram/urun-yoneticisi/route.ts
    src/app/api/telegram/satin-alma/route.ts
    src/app/api/telegram/iade-kalite/route.ts
  </files>
  <action>
Create all 7 webhook routes by copying egitimci/route.ts verbatim and applying the three substitutions from the research substitution table:

| File path | Role enum | Token env var | Log prefix |
|-----------|-----------|---------------|------------|
| tahsilat-uzmani/route.ts | tahsilat_uzmani | TELEGRAM_BOT_TOKEN_TAHSILAT_UZMANI | telegram/tahsilat-uzmani |
| dagitim-koordinatoru/route.ts | dagitim_koordinatoru | TELEGRAM_BOT_TOKEN_DAGITIM_KOORDINATORU | telegram/dagitim-koordinatoru |
| saha-satis/route.ts | saha_satis | TELEGRAM_BOT_TOKEN_SAHA_SATIS | telegram/saha-satis |
| pazarlamaci/route.ts | pazarlamaci | TELEGRAM_BOT_TOKEN_PAZARLAMACI | telegram/pazarlamaci |
| urun-yoneticisi/route.ts | urun_yoneticisi | TELEGRAM_BOT_TOKEN_URUN_YONETICISI | telegram/urun-yoneticisi |
| satin-alma/route.ts | satin_alma | TELEGRAM_BOT_TOKEN_SATIN_ALMA | telegram/satin-alma |
| iade-kalite/route.ts | iade_kalite | TELEGRAM_BOT_TOKEN_IADE_KALITE | telegram/iade-kalite |

For each route, the 3 substitutions in egitimci/route.ts are:
1. `TELEGRAM_BOT_TOKEN_EGITIMCI` → `TELEGRAM_BOT_TOKEN_{ROLE_UPPERCASE}`
2. `dispatchAgentUpdate(update, 'egitimci', botToken)` → `dispatchAgentUpdate(update, '{role_enum}', botToken)`
3. `[telegram/egitimci]` in console.error strings → `[telegram/{log_prefix}]`

The JSDoc comment at the top should be updated to mention the specific role (e.g., "Dedicated webhook for the Tahsilat Uzmani (Collections) agent bot.").

All other code — import statements, idempotency check, after() pattern, HTTP 200 response — is identical.
  </action>
  <verify>ls src/app/api/telegram/ — should show 12 directories (5 existing + 7 new). Run: grep -r "TELEGRAM_BOT_TOKEN_IADE_KALITE" src/app/api/telegram/ — should return iade-kalite/route.ts. Run: grep -r "iade_kalite" src/app/api/telegram/ — should return iade-kalite/route.ts.</verify>
  <done>All 7 route files exist with correct role enum strings and env var names. npx tsc --noEmit shows no errors in the new route files.</done>
</task>

<task type="auto">
  <name>Task 2: Create SS/12-agent-definitions-seed.sql</name>
  <files>SS/12-agent-definitions-seed.sql</files>
  <action>
Create SS/12-agent-definitions-seed.sql following the exact pattern of SS/11-agent-definitions-seed.sql.

The file should:
1. Create unique index if not exists (same as Phase 11 seed)
2. Use DO $$ pattern with v_company_id variable from companies WHERE slug = 'default'
3. INSERT 7 agent_definitions with ON CONFLICT DO UPDATE

Agent definitions to insert:

1. tahsilat_uzmani:
   name: 'Tahsilat Uzmani'
   model: 'claude-haiku-4-5'
   system_prompt: 'Sen bir tahsilat uzmanisin. Sirketin vadesi gecmis alacaklarini takip eder, bayilere odeme hatirlatmasi gonderir ve tahsilat aktivitelerini kayit altina alirsin. Her finansal rakam soylemeden once get_overdue_payments aracini kullan. Asla tahmin yapma. Turkce cevap ver. Profesyonel ama anlayisli bir ton kullan.'

2. dagitim_koordinatoru:
   name: 'Dagitim Koordinatoru'
   model: 'claude-haiku-4-5'
   system_prompt: 'Sen bir dagitim koordinatorusun. Siparislerin teslimat durumunu takip eder, kargo bilgilerini sorgular ve dagitim rotalarini optimize etmek icin oneri sunarsun. Her siparis bilgisi soylemeden once get_delivery_status aracini kullan. Turkce cevap ver.'

3. saha_satis:
   name: 'Saha Satis Sorumlusu'
   model: 'claude-haiku-4-5'
   system_prompt: 'Sen bir saha satis sorumlususun. Bayi ziyaretlerini planlar, gerceklesen ziyaretleri kayit altina alir ve saha satis faaliyetlerini koordine edersin. Ziyaret planlamak icin plan_visit, ziyaret kaydetmek icin log_visit aracini kullan. Turkce cevap ver.'

4. pazarlamaci:
   name: 'Pazarlamaci'
   model: 'claude-sonnet-4-6'
   system_prompt: 'Sen bir pazarlamacisin. Kampanya performansini analiz eder, bayileri segmentlere ayirir ve yeni kampanya onerileri sunarsun. Her kampanya bilgisi soylemeden once analyze_campaigns aracini kullan. segment_dealers ile bayi segmentasyonu yap. suggest_campaign ile strateji oner. Turkce cevap ver.'

5. urun_yoneticisi:
   name: 'Urun Yoneticisi'
   model: 'claude-haiku-4-5'
   system_prompt: 'Sen bir urun yoneticisisin. Urun katalogu analizleri yapar, fiyat stratejisi onerilerinde bulunur ve bayi urun taleplerini degerlendirir. Her urun performans bilgisi icin analyze_catalog, fiyat onerisi icin suggest_pricing, talep analizi icin analyze_requests aracini kullan. Turkce cevap ver.'

6. satin_alma:
   name: 'Satin Alma Sorumlusu'
   model: 'claude-haiku-4-5'
   system_prompt: 'Sen bir satin alma sorumlususun. Stok yenileme onerileri sunar ve tedarikci siparisleri olusturursun. ONEMLI: Tedarikci siparisi olusturmadan once kullanicidan onay al — confirmed=true olmadan siparis olusturma. suggest_restock ile stok analizi yap. Turkce cevap ver.'

7. iade_kalite:
   name: 'Iade Kalite Sorumlusu'
   model: 'claude-haiku-4-5'
   system_prompt: 'Sen bir iade ve kalite sorumlususun. Bayi iade taleplerini yonetir ve kalite sikayetlerini takip edersin. ONEMLI: Iade olusturmadan once kullanicidan onay al — confirmed=true olmadan iade kaydetme. track_complaint ile sikayetleri sorgula veya kaydet. Turkce cevap ver.'

Use ON CONFLICT (company_id, role) DO UPDATE SET system_prompt = EXCLUDED.system_prompt, model = EXCLUDED.model, name = EXCLUDED.name, is_active = EXCLUDED.is_active for each insert.
  </action>
  <verify>File exists: ls SS/12-agent-definitions-seed.sql. Contains all 7 roles: grep -c "INSERT INTO agent_definitions" SS/12-agent-definitions-seed.sql — must return 7.</verify>
  <done>SS/12-agent-definitions-seed.sql contains all 7 agent INSERT statements with appropriate Turkish system prompts and correct models (pazarlamaci gets Sonnet, rest get Haiku).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Apply seed SQL and verify Vercel deploy</name>
  <action>Apply agent_definitions seed SQL in Supabase Dashboard, commit and deploy webhook routes to Vercel, then verify routes are live.</action>
  <what-built>7 Telegram webhook routes and agent_definitions seed SQL</what-built>
  <how-to-verify>
    1. Run the seed SQL in Supabase Dashboard SQL Editor (https://supabase.com/dashboard/project/neqcuhejmornybmbclwt/sql/new):
       - Copy contents of SS/12-agent-definitions-seed.sql and run it
       - Verify 7 new rows in agent_definitions table: SELECT role, name, model FROM agent_definitions ORDER BY created_at;
    2. Deploy to Vercel: git add . && git commit -m "feat(12-06): add 7 webhook routes and agent definitions seed" && git push
    3. After deploy, confirm routes exist at:
       - https://bayi-yonetimi.vercel.app/api/telegram/tahsilat-uzmani (POST only — GET should 405)
       - https://bayi-yonetimi.vercel.app/api/telegram/iade-kalite (POST only)
       (Do not need to register webhooks yet — that requires actual bot tokens)
  </how-to-verify>
  <resume-signal>Type "routes deployed" when agent_definitions seed applied and Vercel deploy complete, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- 7 new route files exist in src/app/api/telegram/
- Each route has correct env var name and role enum string
- SS/12-agent-definitions-seed.sql has 7 INSERT statements
- Seed applied to Supabase (7 new rows in agent_definitions)
- Vercel deploy successful
</verification>

<success_criteria>
All 7 webhook routes are deployed and accessible. agent_definitions has entries for all 7 new roles. The system is ready for Telegram bot token registration when the user creates the bots.
</success_criteria>

<output>
After completion, create `.planning/phases/12-extended-agent-ecosystem/12-06-SUMMARY.md`
</output>
