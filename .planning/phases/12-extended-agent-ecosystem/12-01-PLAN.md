---
phase: 12-extended-agent-ecosystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/011_phase12_domain_tables.sql
autonomous: false
requirements:
  - TU-04
  - SS-03
  - SA-03
  - IK-03

must_haves:
  truths:
    - "collection_activities table exists with company_id, dealer_id, activity_type, notes, amount_expected, due_date, completed_at"
    - "dealer_visits table exists with company_id, dealer_id, planned_date, actual_date, visit_type, outcome, notes"
    - "sales_targets table exists with company_id, dealer_id, target_amount, achieved_amount, period_start, period_end"
    - "suppliers table exists with company_id, name, contact_name, email, phone, notes, is_active"
    - "purchase_orders table exists with company_id, supplier_id, status, items JSONB, total_amount, notes, ordered_at"
    - "return_requests table exists with company_id, dealer_id, order_id (nullable), reason, status, items JSONB, resolved_at"
    - "quality_complaints table exists with company_id, dealer_id, complaint_type, description, status, resolved_at"
    - "All 7 tables have RLS enabled and company_id indexes"
  artifacts:
    - path: "supabase/migrations/011_phase12_domain_tables.sql"
      provides: "SQL DDL for all 7 Phase 12 domain tables"
      contains: "CREATE TABLE collection_activities"
  key_links:
    - from: "collection_activities"
      to: "companies(id)"
      via: "company_id FK ON DELETE CASCADE"
      pattern: "REFERENCES companies\\(id\\)"
    - from: "purchase_orders"
      to: "suppliers(id)"
      via: "supplier_id FK ON DELETE SET NULL"
      pattern: "REFERENCES suppliers\\(id\\)"
    - from: "return_requests"
      to: "orders(id)"
      via: "order_id FK ON DELETE SET NULL"
      pattern: "REFERENCES orders\\(id\\)"
---

<objective>
Create SQL migration file with all 7 new domain-specific tables required by Phase 12 agents.

Purpose: Agent tools for Tahsilat Uzmani, Saha Satis Sorumlusu, Satin Alma, and Iade/Kalite agents need real database tables with proper company_id scoping, RLS, and indexes. Without these tables, tool handlers will have nowhere to write or read data.

Output: supabase/migrations/011_phase12_domain_tables.sql ready to run in Supabase Dashboard SQL Editor.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/010_agent_tables.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 011_phase12_domain_tables.sql migration file</name>
  <files>supabase/migrations/011_phase12_domain_tables.sql</files>
  <action>
Create the migration file with all 7 domain tables following the exact pattern from 010_agent_tables.sql:
- Each table: id UUID PRIMARY KEY DEFAULT gen_random_uuid(), company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE, timestamps, ALTER TABLE xxx ENABLE ROW LEVEL SECURITY, CREATE INDEX idx_xxx_company ON xxx(company_id, created_at DESC)

Tables to create in order (respecting FK dependencies — suppliers before purchase_orders):

1. collection_activities: company_id, dealer_id UUID NOT NULL REFERENCES dealers(id) ON DELETE CASCADE, activity_type TEXT NOT NULL CHECK (activity_type IN ('reminder_sent','call_made','visit','payment_received','note')), notes TEXT, amount_expected NUMERIC(12,2), due_date DATE, completed_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW()

2. dealer_visits: company_id, dealer_id UUID NOT NULL REFERENCES dealers(id) ON DELETE CASCADE, planned_date DATE NOT NULL, actual_date DATE, visit_type TEXT NOT NULL DEFAULT 'routine' CHECK (visit_type IN ('routine','sales','complaint','delivery')), outcome TEXT, notes TEXT, created_at TIMESTAMPTZ DEFAULT NOW()

3. sales_targets: company_id, dealer_id UUID NOT NULL REFERENCES dealers(id) ON DELETE CASCADE, target_amount NUMERIC(12,2) NOT NULL, achieved_amount NUMERIC(12,2) NOT NULL DEFAULT 0, period_start DATE NOT NULL, period_end DATE NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW()

4. suppliers: company_id, name TEXT NOT NULL, contact_name TEXT, email TEXT, phone TEXT, notes TEXT, is_active BOOLEAN DEFAULT true, created_at TIMESTAMPTZ DEFAULT NOW()

5. purchase_orders: company_id, supplier_id UUID REFERENCES suppliers(id) ON DELETE SET NULL, status TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','sent','confirmed','received','cancelled')), items JSONB NOT NULL DEFAULT '[]', total_amount NUMERIC(12,2), notes TEXT, ordered_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW()

6. return_requests: company_id, dealer_id UUID NOT NULL REFERENCES dealers(id) ON DELETE CASCADE, order_id UUID REFERENCES orders(id) ON DELETE SET NULL, reason TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','approved','rejected','completed')), items JSONB NOT NULL DEFAULT '[]', resolved_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW()

7. quality_complaints: company_id, dealer_id UUID NOT NULL REFERENCES dealers(id) ON DELETE CASCADE, complaint_type TEXT NOT NULL CHECK (complaint_type IN ('product_quality','delivery','packaging','other')), description TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'open' CHECK (status IN ('open','investigating','resolved','closed')), resolved_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW()

For each table after creation, add:
  ALTER TABLE {table_name} ENABLE ROW LEVEL SECURITY;
  CREATE INDEX idx_{table_name}_company ON {table_name}(company_id, created_at DESC);

Add a comment header: -- Phase 12 Domain Tables Migration
-- Run in Supabase Dashboard SQL Editor (neqcuhejmornybmbclwt)
-- Tables: collection_activities, dealer_visits, sales_targets, suppliers, purchase_orders, return_requests, quality_complaints
  </action>
  <verify>File exists at supabase/migrations/011_phase12_domain_tables.sql and contains all 7 CREATE TABLE statements. Run: grep -c "CREATE TABLE" supabase/migrations/011_phase12_domain_tables.sql — must return 7.</verify>
  <done>SQL file contains all 7 tables with company_id FKs, RLS enable, and indexes. File is ready to paste into Supabase Dashboard SQL Editor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Run migration in Supabase Dashboard</name>
  <action>Run SQL migration in Supabase Dashboard and verify all 7 tables exist with RLS enabled.</action>
  <what-built>SQL migration file with 7 new domain tables for Phase 12 agents</what-built>
  <how-to-verify>
    1. Open Supabase Dashboard SQL Editor: https://supabase.com/dashboard/project/neqcuhejmornybmbclwt/sql/new
    2. Copy the contents of supabase/migrations/011_phase12_domain_tables.sql
    3. Paste and run in SQL Editor
    4. Verify all 7 tables appear in Table Editor: collection_activities, dealer_visits, sales_targets, suppliers, purchase_orders, return_requests, quality_complaints
    5. Check that each table has RLS enabled (shield icon visible)
    6. Run: SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' AND table_name IN ('collection_activities','dealer_visits','sales_targets','suppliers','purchase_orders','return_requests','quality_complaints') ORDER BY table_name; — should return 7 rows
  </how-to-verify>
  <resume-signal>Type "tables created" when all 7 tables exist in Supabase, or describe any SQL errors encountered</resume-signal>
</task>

</tasks>

<verification>
- supabase/migrations/011_phase12_domain_tables.sql exists with all 7 CREATE TABLE statements
- Each table has company_id NOT NULL REFERENCES companies(id) ON DELETE CASCADE
- RLS enabled on all 7 tables
- Indexes created for (company_id, created_at DESC) on all 7 tables
- FK dependencies satisfied: suppliers before purchase_orders, orders referenced by return_requests
</verification>

<success_criteria>
All 7 domain tables (collection_activities, dealer_visits, sales_targets, suppliers, purchase_orders, return_requests, quality_complaints) exist in the Supabase database, are company-scoped, and have RLS enabled.
</success_criteria>

<output>
After completion, create `.planning/phases/12-extended-agent-ecosystem/12-01-SUMMARY.md`
</output>
