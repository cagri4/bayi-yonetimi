---
phase: 11-financial-operations-agents
plan: 04
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
  - 11-03
files_modified:
  - src/lib/agents/tool-registry.ts
  - src/lib/agents/dispatcher.ts
  - src/app/api/telegram/muhasebeci/route.ts
  - src/app/api/telegram/depo-sorumlusu/route.ts
  - src/app/api/telegram/genel-mudur/route.ts
  - SS/11-agent-definitions-seed.sql
autonomous: false
requirements:
  - MH-06
  - DS-03
  - GM-04

user_setup:
  - service: supabase-dashboard
    why: "Seed agent_definitions table with system prompts for 3 new agents"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN_MUHASEBECI
        source: "BotFather -> Create new bot for Muhasebeci"
      - name: TELEGRAM_BOT_TOKEN_DEPO_SORUMLUSU
        source: "BotFather -> Create new bot for Depo Sorumlusu"
      - name: TELEGRAM_BOT_TOKEN_GENEL_MUDUR
        source: "BotFather -> Create new bot for Genel Mudur Danismani"
    dashboard_config:
      - task: "Execute agent_definitions SQL seed"
        location: "Supabase Dashboard -> SQL Editor"

must_haves:
  truths:
    - "TOOL_REGISTRY maps muhasebeci, depo_sorumlusu, and genel_mudur_danismani to their real tool arrays (not placeholderTools)"
    - "Dispatcher has 3 new else-if branches calling createMuhasebeciHandlers, createDepoSorumlusuHandlers, and createGenelMudurHandlers"
    - "Three new webhook routes exist at /api/telegram/muhasebeci, /api/telegram/depo-sorumlusu, and /api/telegram/genel-mudur"
    - "Each webhook route uses its own TELEGRAM_BOT_TOKEN_{ROLE} env var"
    - "SQL seed contains system prompts enforcing MH-06 (hallucination prevention) and DS-03 (stock update confirmation)"
    - "Muhasebeci system prompt includes KRITIK KURAL about never stating financial numbers without tool call"
    - "Depo Sorumlusu system prompt instructs explicit confirmation before update_stock"
    - "Genel Mudur system prompt instructs cross-domain analysis using Sonnet 4.6 reasoning"
  artifacts:
    - path: "src/lib/agents/tool-registry.ts"
      provides: "Updated TOOL_REGISTRY with 3 new real tool arrays"
      contains: "muhasebeciTools"
    - path: "src/lib/agents/dispatcher.ts"
      provides: "3 new role branches in tool handler switch"
      contains: "createMuhasebeciHandlers"
    - path: "src/app/api/telegram/muhasebeci/route.ts"
      provides: "Muhasebeci bot webhook"
      contains: "TELEGRAM_BOT_TOKEN_MUHASEBECI"
    - path: "src/app/api/telegram/depo-sorumlusu/route.ts"
      provides: "Depo Sorumlusu bot webhook"
      contains: "TELEGRAM_BOT_TOKEN_DEPO_SORUMLUSU"
    - path: "src/app/api/telegram/genel-mudur/route.ts"
      provides: "Genel Mudur Danismani bot webhook"
      contains: "TELEGRAM_BOT_TOKEN_GENEL_MUDUR"
    - path: "SS/11-agent-definitions-seed.sql"
      provides: "SQL seed for 3 agent definitions with Turkish system prompts"
      contains: "ON CONFLICT (company_id, role) DO UPDATE"
  key_links:
    - from: "src/lib/agents/tool-registry.ts"
      to: "src/lib/agents/tools/muhasebeci-tools.ts"
      via: "import { muhasebeciTools }"
      pattern: "import.*muhasebeciTools.*muhasebeci-tools"
    - from: "src/lib/agents/dispatcher.ts"
      to: "src/lib/agents/tools/muhasebeci-tools.ts"
      via: "import { createMuhasebeciHandlers }"
      pattern: "import.*createMuhasebeciHandlers"
    - from: "src/app/api/telegram/muhasebeci/route.ts"
      to: "src/lib/agents/dispatcher.ts"
      via: "dispatchAgentUpdate(update, 'muhasebeci', botToken)"
      pattern: "dispatchAgentUpdate.*muhasebeci"
---

<objective>
Wire all three Phase 11 agents into the existing infrastructure — update TOOL_REGISTRY, extend the dispatcher's role switch, create 3 dedicated Telegram webhook routes, and prepare the SQL seed with Turkish system prompts enforcing hallucination prevention and stock update confirmation.

Purpose: This is the integration plan that connects the tool files from Plans 01-03 to the live Telegram pipeline. After this plan, the three new agents are functionally complete and ready for end-to-end testing once bot tokens and SQL seed are configured.

Output: Updated tool-registry.ts, updated dispatcher.ts, 3 new webhook routes, 1 SQL seed file.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-financial-operations-agents/11-RESEARCH.md
@.planning/phases/11-financial-operations-agents/11-01-SUMMARY.md
@.planning/phases/11-financial-operations-agents/11-02-SUMMARY.md
@.planning/phases/11-financial-operations-agents/11-03-SUMMARY.md

# Files to modify
@src/lib/agents/tool-registry.ts
@src/lib/agents/dispatcher.ts

# Existing webhook route pattern to replicate
@src/app/api/telegram/egitimci/route.ts

# SQL seed pattern from Phase 10
@SS/10-agent-definitions-seed.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ToolRegistry and extend dispatcher with 3 new agent roles</name>
  <files>src/lib/agents/tool-registry.ts, src/lib/agents/dispatcher.ts</files>
  <action>
**tool-registry.ts changes:**
1. Add imports at top:
   ```
   import { muhasebeciTools } from './tools/muhasebeci-tools'
   import { depoSorumlusuTools } from './tools/depo-sorumlusu-tools'
   import { genelMudurTools } from './tools/genel-mudur-tools'
   ```
2. Update TOOL_REGISTRY entries:
   - `muhasebeci: muhasebeciTools` (was `placeholderTools`)
   - `depo_sorumlusu: depoSorumlusuTools` (was `placeholderTools`)
   - `genel_mudur_danismani: genelMudurTools` (was `placeholderTools`)
3. Leave all other entries unchanged

**dispatcher.ts changes:**
1. Add imports at top (alongside existing egitimci/satis imports):
   ```
   import { createMuhasebeciHandlers } from './tools/muhasebeci-tools'
   import { createDepoSorumlusuHandlers } from './tools/depo-sorumlusu-tools'
   import { createGenelMudurHandlers } from './tools/genel-mudur-tools'
   ```
2. Extend the role switch block (after satis_temsilcisi, before the `else` fallback):
   ```
   } else if (role === 'muhasebeci') {
     toolHandlers = createMuhasebeciHandlers(supabase)
   } else if (role === 'depo_sorumlusu') {
     toolHandlers = createDepoSorumlusuHandlers(supabase)
   } else if (role === 'genel_mudur_danismani') {
     toolHandlers = createGenelMudurHandlers(supabase)
   } else {
   ```
3. Do NOT change any other part of dispatcher.ts
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors.
Verify TOOL_REGISTRY no longer uses placeholderTools for the 3 new roles.
  </verify>
  <done>
ToolRegistry maps muhasebeci, depo_sorumlusu, and genel_mudur_danismani to their real tool arrays. Dispatcher routes all 3 roles to their factory-built handler maps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 3 dedicated webhook routes and SQL seed file</name>
  <files>src/app/api/telegram/muhasebeci/route.ts, src/app/api/telegram/depo-sorumlusu/route.ts, src/app/api/telegram/genel-mudur/route.ts, SS/11-agent-definitions-seed.sql</files>
  <action>
**3 webhook routes** — each is a copy of `src/app/api/telegram/egitimci/route.ts` with these substitutions:

1. `/api/telegram/muhasebeci/route.ts`:
   - Log prefix: `[telegram/muhasebeci]`
   - Env var: `process.env.TELEGRAM_BOT_TOKEN_MUHASEBECI || ''`
   - Dispatch: `dispatchAgentUpdate(update, 'muhasebeci', botToken)`

2. `/api/telegram/depo-sorumlusu/route.ts`:
   - Log prefix: `[telegram/depo-sorumlusu]`
   - Env var: `process.env.TELEGRAM_BOT_TOKEN_DEPO_SORUMLUSU || ''`
   - Dispatch: `dispatchAgentUpdate(update, 'depo_sorumlusu', botToken)`
   - Note: URL path uses kebab-case (`depo-sorumlusu`) but the role enum uses underscores (`depo_sorumlusu`)

3. `/api/telegram/genel-mudur/route.ts`:
   - Log prefix: `[telegram/genel-mudur]`
   - Env var: `process.env.TELEGRAM_BOT_TOKEN_GENEL_MUDUR || ''`
   - Dispatch: `dispatchAgentUpdate(update, 'genel_mudur_danismani', botToken)`
   - Note: URL path uses shortened kebab-case (`genel-mudur`) but the role enum is full (`genel_mudur_danismani`)

Each route includes the same idempotency pattern, after() background processing, and force-dynamic export as the egitimci route.

**SQL seed file** — `SS/11-agent-definitions-seed.sql`:

Header comment listing required env vars:
```sql
-- Phase 11: Financial and Operations Agent Definitions
-- Required Vercel env vars:
--   TELEGRAM_BOT_TOKEN_MUHASEBECI
--   TELEGRAM_BOT_TOKEN_DEPO_SORUMLUSU
--   TELEGRAM_BOT_TOKEN_GENEL_MUDUR
--
-- Execute in Supabase Dashboard SQL Editor:
-- https://supabase.com/dashboard/project/neqcuhejmornybmbclwt/sql/new
```

INSERT 3 agent definitions using the existing company UUID from `SELECT id FROM companies WHERE slug = 'default'`:

1. **Muhasebeci** (MH-06 enforcement):
   - role: 'muhasebeci', name: 'Muhasebeci', model: 'claude-sonnet-4-6'
   - system_prompt: Turkish prompt including:
     - "Sen bir bayi muhasebe asistanisin. Bayilerin finansal sorularina yardimci olursun."
     - "KRITIK KURAL: Hicbir finansal rakam (bakiye, borc, alacak, fatura tutari, odeme miktari) vermeden once MUTLAKA ilgili araci cagir. Tahmin yapma, 'yaklasik' deme, gecmis konusmalardan rakam tekrarlama. Her zaman araci cagir, sonucu al, sonra yanit ver."
     - "Kullanilabilir araclar: get_financials, get_payment_history, get_invoices, get_dealer_balance, export_report"
     - "Her zaman Turkce cevap ver."

2. **Depo Sorumlusu** (DS-03 confirmation):
   - role: 'depo_sorumlusu', name: 'Depo Sorumlusu', model: 'claude-haiku-4-5'
   - system_prompt: Turkish prompt including:
     - "Sen bir depo sorumlususun. Stok durumu, siparis hazirlama ve sevkiyat sorularina yardimci olursun."
     - "ONEMLI KURAL: update_stock aracini cagirmadan once, bayiye guncellenecek urun adini, mevcut stok miktarini ve yeni stok miktarini goster. Bayinin acik onayini al (evet, onayliyorum, tamam gibi). Onay alinmadan update_stock aracini ASLA cagirma."
     - "Kullanilabilir araclar: get_inventory_status, get_pending_orders, update_stock, check_reorder_level, get_shipments"
     - "Her zaman Turkce cevap ver."

3. **Genel Mudur Danismani** (GM-04 complex analysis):
   - role: 'genel_mudur_danismani', name: 'Genel Mudur Danismani', model: 'claude-sonnet-4-6'
   - system_prompt: Turkish prompt including:
     - "Sen bir genel mudur danismanisin. Sirketin genel durumu, KPI'lar, trend analizleri ve stratejik oneriler sunarsun."
     - "Farkli alanlardaki verileri birlestirerek kapsamli analizler yapabilirsin: finansal veriler, satis verileri, stok durumu ve bayi performansi."
     - "KPI ve trend analizi yaparken: mutlaka once ilgili araclari cagir, verileri topla, sonra yorumla. Trend icin onceki donemlerle karsilastirma yap. Somut rakamlarla destekle."
     - "Kullanilabilir araclar: get_financials, get_payment_history, get_dealer_balance, get_any_dealer_balance, get_catalog, get_order_status, get_campaigns, check_stock, get_dashboard_summary, export_report"
     - "Her zaman Turkce cevap ver."

Use `ON CONFLICT (company_id, role) DO UPDATE SET system_prompt = EXCLUDED.system_prompt, model = EXCLUDED.model, is_active = EXCLUDED.is_active` — same pattern as Phase 10 seed.
  </action>
  <verify>
Run `npx tsc --noEmit` — zero errors.
Run `npm run build` — verify 3 new routes appear in build output.
Verify SQL file exists and contains 3 INSERT values.
  </verify>
  <done>
Three webhook routes created following the exact Phase 10 pattern. SQL seed prepared with Turkish system prompts enforcing MH-06 hallucination prevention and DS-03 stock update confirmation. Build succeeds with all new routes.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Execute SQL seed in Supabase Dashboard</name>
  <files>SS/11-agent-definitions-seed.sql</files>
  <action>
Execute the SQL seed file in the Supabase Dashboard SQL Editor. This seeds 3 agent definitions (Muhasebeci, Depo Sorumlusu, Genel Mudur Danismani) with Turkish system prompts that enforce MH-06 hallucination prevention and DS-03 stock update confirmation.

Steps:
1. Open: https://supabase.com/dashboard/project/neqcuhejmornybmbclwt/sql/new
2. Copy contents of `SS/11-agent-definitions-seed.sql`
3. Paste into the SQL Editor and click "Run"
4. Verify: Run `SELECT role, name, model, is_active FROM agent_definitions WHERE role IN ('muhasebeci', 'depo_sorumlusu', 'genel_mudur_danismani');`
5. Should show 3 rows: muhasebeci (sonnet), depo_sorumlusu (haiku), genel_mudur_danismani (sonnet)
  </action>
  <verify>
Run verification query: `SELECT role, name, model, is_active FROM agent_definitions WHERE role IN ('muhasebeci', 'depo_sorumlusu', 'genel_mudur_danismani');` — should return 3 rows.
  </verify>
  <done>
3 agent definitions seeded in database with correct models and Turkish system prompts.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` shows routes: /api/telegram/muhasebeci, /api/telegram/depo-sorumlusu, /api/telegram/genel-mudur
- TOOL_REGISTRY no longer uses placeholderTools for muhasebeci, depo_sorumlusu, genel_mudur_danismani
- Dispatcher has 5 role branches: egitimci, satis_temsilcisi, muhasebeci, depo_sorumlusu, genel_mudur_danismani
- SQL seed contains KRITIK KURAL in muhasebeci system prompt (MH-06)
- SQL seed contains update_stock confirmation rule in depo_sorumlusu system prompt (DS-03)
- SQL seed uses ON CONFLICT (company_id, role) DO UPDATE pattern
</verification>

<success_criteria>
All Phase 11 agents are wired into the Telegram pipeline. The ToolRegistry, dispatcher, webhook routes, and agent definitions are complete. After bot token configuration and SQL seed execution, the three agents are live.
</success_criteria>

<output>
After completion, create `.planning/phases/11-financial-operations-agents/11-04-SUMMARY.md`
</output>
