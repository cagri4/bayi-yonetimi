---
phase: 02-order-management-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/002_realtime_setup.sql
  - src/app/(dealer)/orders/page.tsx
  - src/app/(dealer)/orders/[id]/page.tsx
  - src/components/orders/order-status-badge.tsx
  - src/components/orders/order-status-timeline.tsx
  - src/hooks/use-order-realtime.ts
autonomous: true

must_haves:
  truths:
    - "Bayi gecmis siparislerini liste halinde gorebilir"
    - "Bayi siparis detayini ve durum gecmisini gorebilir"
    - "Bayi siparis durumu degistiginde anlik bildirim alir"
  artifacts:
    - path: "supabase/migrations/002_realtime_setup.sql"
      provides: "Database trigger for status history + Realtime permissions"
      contains: "track_order_status_change"
    - path: "src/app/(dealer)/orders/page.tsx"
      provides: "Order list page for dealers"
      min_lines: 50
    - path: "src/app/(dealer)/orders/[id]/page.tsx"
      provides: "Order detail page with timeline"
      min_lines: 80
    - path: "src/hooks/use-order-realtime.ts"
      provides: "Realtime subscription hook"
      exports: ["useOrderRealtime"]
  key_links:
    - from: "src/app/(dealer)/orders/[id]/page.tsx"
      to: "src/hooks/use-order-realtime.ts"
      via: "useOrderRealtime hook import"
      pattern: "useOrderRealtime"
    - from: "src/hooks/use-order-realtime.ts"
      to: "supabase.channel"
      via: "Realtime postgres_changes subscription"
      pattern: "postgres_changes.*orders"
---

<objective>
Dealer order history list and order detail with realtime status tracking

Purpose: Enable dealers to view their order history and track order status changes in real-time with instant notifications
Output: Order list page, order detail page with status timeline, realtime subscription hook, database trigger for automatic status history
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-order-management-tracking/02-RESEARCH.md

# Phase 1 foundations
@src/lib/supabase/client.ts
@src/lib/supabase/server.ts
@src/lib/actions/orders.ts
@supabase/migrations/001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database trigger and Realtime permissions</name>
  <files>supabase/migrations/002_realtime_setup.sql</files>
  <action>
Create SQL migration file with:

1. Database trigger function `track_order_status_change()`:
   - Fires AFTER INSERT OR UPDATE OF status_id ON orders
   - Inserts into order_status_history: order_id, status_id, changed_by (auth.uid()), notes
   - Uses SECURITY DEFINER to access auth.uid()
   - Only logs if status actually changed (OLD.status_id IS DISTINCT FROM NEW.status_id) or on INSERT

2. Create trigger `order_status_change_trigger` on orders table

3. Grant Realtime permissions:
   - GRANT SELECT ON orders TO supabase_realtime;
   - GRANT SELECT ON order_status_history TO supabase_realtime;
   - GRANT SELECT ON order_statuses TO supabase_realtime;

4. Add tables to Realtime publication:
   - ALTER PUBLICATION supabase_realtime ADD TABLE orders;
   - ALTER PUBLICATION supabase_realtime ADD TABLE order_status_history;

Reference 02-RESEARCH.md Pattern 2 for trigger implementation.
Turkish notes: 'Siparis olusturuldu' for INSERT, 'Durum degistirildi' for UPDATE.
  </action>
  <verify>
SQL file exists with valid PostgreSQL syntax:
- Contains CREATE OR REPLACE FUNCTION track_order_status_change
- Contains CREATE TRIGGER order_status_change_trigger
- Contains GRANT SELECT statements for supabase_realtime role
- Contains ALTER PUBLICATION statements
  </verify>
  <done>
Migration file ready to be applied to Supabase. Trigger will auto-create status history entries on order status changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Realtime subscription hook and order components</name>
  <files>
src/hooks/use-order-realtime.ts
src/components/orders/order-status-badge.tsx
src/components/orders/order-status-timeline.tsx
  </files>
  <action>
1. Create `use-order-realtime.ts` hook:
   - Accept orderId parameter
   - Use createClient() from @/lib/supabase/client
   - Subscribe to postgres_changes on 'orders' table filtered by id=eq.${orderId}
   - On UPDATE event: show toast notification with status name, call router.refresh()
   - CRITICAL: Return cleanup function that calls supabase.removeChannel(channel)
   - Handle React Strict Mode double-mount (store channel in ref)
   - Return { isSubscribed } state
   Reference 02-RESEARCH.md Pattern 1.

2. Create `order-status-badge.tsx`:
   - Accept status: { code: string, name: string } prop
   - Use shadcn Badge component with variant based on status.code:
     - pending: default (gray)
     - confirmed: secondary (blue)
     - preparing: outline (yellow)
     - shipped: default (indigo)
     - delivered: success (green) - use className for green since Badge doesn't have success variant
     - cancelled: destructive (red)
   - Display status.name (Turkish)

3. Create `order-status-timeline.tsx`:
   - Accept history array with { id, status: { code, name }, created_at, notes? }
   - Accept currentStatusCode prop
   - Use date-fns formatDistanceToNow with { locale: tr, addSuffix: true }
   - Display timeline with lucide icons: CheckCircle2 (completed), Clock (current), Circle (pending)
   - Connect steps with vertical line colored based on completion state
   Reference 02-RESEARCH.md Pattern 6.
  </action>
  <verify>
- npm run build passes without TypeScript errors
- Files exist with correct exports
- Hook uses useEffect cleanup pattern
  </verify>
  <done>
Realtime hook and order display components ready for use in order pages.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create dealer order list and detail pages</name>
  <files>
src/app/(dealer)/orders/page.tsx
src/app/(dealer)/orders/[id]/page.tsx
  </files>
  <action>
1. Create `orders/page.tsx` (Order List):
   - Server component using createClient from @/lib/supabase/server
   - Call getDealerOrders() from @/lib/actions/orders (already exists)
   - Display orders in a table with columns: Order Number, Date, Status, Total, Actions
   - Use OrderStatusBadge for status column
   - Use date-fns format with Turkish locale for dates
   - Link to order detail page
   - Handle empty state: "Henuz siparisizin bulunmuyor"
   - Add page title: "Siparislerim"

2. Create `orders/[id]/page.tsx` (Order Detail):
   - Server component fetching order with items and status history
   - Query order with: status:order_statuses(*), items:order_items(*), history:order_status_history(*, status:order_statuses(*))
   - Client component wrapper for Realtime subscription
   - Display:
     - Order header: order_number, created_at, status badge
     - Order items table: product_name, product_code, quantity, unit_price, total_price
     - Order totals: subtotal, discount_amount, total_amount
     - Status timeline using OrderStatusTimeline component
     - Notes if present
   - Use useOrderRealtime hook for live updates
   - Add back link to orders list

Turkish labels:
- "Siparis No", "Tarih", "Durum", "Toplam", "Detay"
- "Urun", "Kod", "Adet", "Birim Fiyat", "Tutar"
- "Ara Toplam", "Indirim", "Genel Toplam"
- "Siparis Notlari", "Siparis Gecmisi"
  </action>
  <verify>
- npm run build passes
- Navigate to /orders shows order list
- Navigate to /orders/[id] shows order detail with timeline
  </verify>
  <done>
Dealers can view their order history and track individual order status with realtime updates.
  </done>
</task>

</tasks>

<verification>
1. Database migration file contains trigger and Realtime setup
2. All components compile without TypeScript errors
3. Order list page displays dealer's orders
4. Order detail page shows items and status timeline
5. Realtime hook properly cleans up subscriptions
</verification>

<success_criteria>
- Dealer can navigate to /orders and see list of their orders
- Dealer can click an order to see full details including items and status history
- OrderStatusBadge shows correct colors for different statuses
- OrderStatusTimeline displays chronological status changes
- useOrderRealtime hook subscribes to order changes with proper cleanup
- Migration file ready for manual application to Supabase
</success_criteria>

<output>
After completion, create `.planning/phases/02-order-management-tracking/02-01-SUMMARY.md`
</output>
