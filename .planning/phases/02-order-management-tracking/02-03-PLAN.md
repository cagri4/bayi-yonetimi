---
phase: 02-order-management-tracking
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(dealer)/quick-order/page.tsx
  - src/components/quick-order/quick-order-form.tsx
  - src/components/quick-order/frequent-products.tsx
  - src/components/orders/reorder-button.tsx
  - src/lib/queries/orders.ts
autonomous: true

must_haves:
  truths:
    - "Bayi gecmis siparisinden tekrar siparis verebilir"
    - "Bayi sik siparis ettigi urunlerden hizli siparis verebilir"
    - "Bayi urun kodunu girerek hizli urun arayabilir"
  artifacts:
    - path: "src/components/orders/reorder-button.tsx"
      provides: "Reorder button that adds past order items to cart"
      min_lines: 30
    - path: "src/app/(dealer)/quick-order/page.tsx"
      provides: "Quick order page with SKU search"
      min_lines: 40
    - path: "src/components/quick-order/quick-order-form.tsx"
      provides: "Multi-row quick order form"
      min_lines: 80
    - path: "src/lib/queries/orders.ts"
      provides: "Frequent products query"
      exports: ["getFrequentProducts"]
  key_links:
    - from: "src/components/orders/reorder-button.tsx"
      to: "get_dealer_price"
      via: "RPC call for current prices"
      pattern: "get_dealer_price"
    - from: "src/components/quick-order/quick-order-form.tsx"
      to: "src/store/cart.ts"
      via: "Zustand cart store"
      pattern: "useCartStore"
---

<objective>
Quick order form with SKU search and reorder from past orders

Purpose: Enable dealers to quickly place orders using product codes (SKU) and reorder items from their order history with current pricing
Output: Quick order form with multi-row SKU entry, frequent products display, reorder button for past orders
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-order-management-tracking/02-RESEARCH.md

# Phase 1 foundations
@src/lib/supabase/client.ts
@src/lib/supabase/server.ts
@src/store/cart.ts
@src/lib/actions/orders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reorder button component</name>
  <files>
src/components/orders/reorder-button.tsx
src/app/(dealer)/orders/[id]/page.tsx
  </files>
  <action>
1. Create `reorder-button.tsx` client component:
   - Accept orderId prop
   - Use useCartStore from @/store/cart for addItem
   - On click:
     a. Fetch order items from order_items table filtered by order_id
     b. Get current user and dealer id
     c. For each item, call get_dealer_price RPC to get CURRENT price (not historical)
     d. Add each item to cart using addItem with current price
     e. Show toast: "${count} urun sepete eklendi"
     f. Handle errors: "Siparis yuklenirken hata olustu"
   - Show loading state during operation
   - Button text: "Tekrar Siparis Ver"
   - Use lucide-react RefreshCw or ShoppingCart icon
   Reference 02-RESEARCH.md Pattern 4.

CRITICAL: Must fetch CURRENT prices via get_dealer_price, not use historical prices from order_items.
This ensures dealers pay current prices even when reordering old orders.

2. Update `orders/[id]/page.tsx` to include ReorderButton:
   - Add ReorderButton below order details or in header
   - Pass orderId prop
  </action>
  <verify>
- npm run build passes
- ReorderButton component exists with correct exports
- Order detail page includes reorder button
  </verify>
  <done>
Dealers can reorder past orders with one click, items added to cart at current prices.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create frequent products query and quick order form</name>
  <files>
src/lib/queries/orders.ts
src/components/quick-order/quick-order-form.tsx
src/components/quick-order/frequent-products.tsx
  </files>
  <action>
1. Create `queries/orders.ts` with getFrequentProducts:
   - Accept dealerId and optional limit (default 10)
   - Query order_items joined with orders for this dealer
   - Filter to last 90 days
   - Group by product_id, aggregate: count orders, sum quantities
   - Sort by order count descending
   - Return top N products with: product_id, product_code, product_name, total_quantity, order_count
   - For each product, get current price via get_dealer_price
   Reference 02-RESEARCH.md Code Examples - Frequent Products Query.

2. Create `frequent-products.tsx` client component:
   - Accept products array from server
   - Display as grid/list of product cards
   - Each card shows: product name, code, order count, "X kez siparis verildi"
   - Quick add button that adds to cart with quantity 1
   - Use useCartStore addItem

3. Create `quick-order-form.tsx` client component:
   - State: array of rows [{ id, sku, productName, productId, quantity, price }]
   - Start with 5 empty rows
   - Each row has:
     - SKU input (product code)
     - Product name display (auto-filled on SKU search)
     - Quantity input (number, default 1)
     - Price display (auto-filled)
     - Remove row button (if more than 1 row)
   - On SKU blur:
     a. Search products by code using .ilike('code', sku)
     b. If found, get dealer price via get_dealer_price
     c. Update row with productId, productName, price
     d. If not found, show inline error "Urun bulunamadi"
   - Add row button at bottom
   - "Sepete Ekle" button:
     a. Filter rows with valid productId and quantity > 0
     b. Add all to cart
     c. Show toast: "${count} urun sepete eklendi"
     d. Reset form to empty rows
   Reference 02-RESEARCH.md Pattern 5.

Turkish labels:
- "Urun Kodu", "Urun Adi", "Adet", "Fiyat"
- "Satir Ekle", "Sepete Ekle", "Temizle"
- "Urun bulunamadi"
  </action>
  <verify>
- npm run build passes
- All files exist with correct exports
- SKU search works with product code lookup
  </verify>
  <done>
Quick order form allows multi-row SKU entry with auto-product lookup and bulk add to cart.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create quick order page</name>
  <files>
src/app/(dealer)/quick-order/page.tsx
src/app/(dealer)/layout.tsx
  </files>
  <action>
1. Create `quick-order/page.tsx`:
   - Server component
   - Get current dealer id
   - Call getFrequentProducts to load top 10 frequent products
   - Render page with two sections:
     a. "Sik Siparis Edilen Urunler" - FrequentProducts component
     b. "Hizli Siparis Formu" - QuickOrderForm component
   - Page title: "Hizli Siparis"
   - Add description text explaining the feature

2. Update dealer layout.tsx to add navigation link:
   - Add "Hizli Siparis" link to navigation menu
   - Use lucide-react Zap icon
   - Place between "Katalog" and "Sepet" in nav order

Layout for quick order page:
- Header: "Hizli Siparis"
- Section 1: Frequent products grid (if any)
- Section 2: Quick order form table
- Both sections in responsive layout (stack on mobile, side-by-side on desktop)
  </action>
  <verify>
- npm run build passes
- Navigate to /quick-order shows the page
- Frequent products display if dealer has order history
- Quick order form allows SKU entry and cart addition
  </verify>
  <done>
Complete quick order experience with frequent products and SKU-based ordering.
  </done>
</task>

</tasks>

<verification>
1. Reorder button adds items to cart with current prices
2. Quick order form searches products by SKU
3. Frequent products shows top ordered items
4. All cart additions work correctly with Zustand store
5. Navigation includes quick order link
</verification>

<success_criteria>
- Reorder button on order detail page works and uses current prices
- Quick order page accessible from dealer navigation
- Frequent products section shows dealer's most ordered items (last 90 days)
- SKU search finds products by code and shows price
- Multi-row form allows adding multiple products at once
- All items correctly added to Zustand cart store
- Proper error handling for invalid SKUs
- All labels and messages in Turkish
</success_criteria>

<output>
After completion, create `.planning/phases/02-order-management-tracking/02-03-SUMMARY.md`
</output>
