---
phase: 08-multi-tenant-db-migration
plan: "05"
type: execute
wave: 4
depends_on:
  - "08-04"
files_modified:
  - src/types/database.types.ts
autonomous: true
requirements:
  - MT-01
  - MT-02
  - MT-04
  - MT-05

must_haves:
  truths:
    - "database.types.ts has a 'companies' table type with Row/Insert/Update/Relationships"
    - "users.role type is 'admin' | 'dealer' | 'superadmin' (not just admin | dealer)"
    - "users.company_id field is present in Row (string | null) and Insert/Update"
    - "All 19 tenant-scoped tables have company_id: string in their Row type"
    - "All 19 tenant-scoped tables have company_id: string in their Insert type (required)"
    - "All 19 tenant-scoped tables have company_id?: string in their Update type (optional)"
    - "npx tsc --noEmit passes with no type errors after update"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "TypeScript types matching post-migration schema"
      contains: "companies"
  key_links:
    - from: "src/types/database.types.ts"
      to: "Database['public']['Tables']['companies']"
      via: "TypeScript interface"
      pattern: "companies.*Row"
    - from: "users Row type"
      to: "role: 'admin' | 'dealer' | 'superadmin'"
      via: "union type"
      pattern: "superadmin"
---

<objective>
Update src/types/database.types.ts to reflect the post-migration schema: add companies table type, add company_id to all 19 tenant-scoped tables, update users.role to include 'superadmin', and add users.company_id field.

Purpose: TypeScript types are the application's contract with the database. After the migration, all tables have company_id — the types must match or every Supabase query using the typed client will produce TypeScript errors. This plan also verifies the build compiles cleanly.

Output: Updated src/types/database.types.ts with zero TypeScript errors.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-tenant-db-migration/08-RESEARCH.md
@.planning/phases/08-multi-tenant-db-migration/08-04-SUMMARY.md
@src/types/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add companies table type and update users type in database.types.ts</name>
  <files>src/types/database.types.ts</files>
  <action>
Read the full src/types/database.types.ts file first. Then make the following targeted edits:

**Edit 1: Update users table type**

Find the `users` table type definition. Update:
- `role` in Row from `'admin' | 'dealer'` to `'admin' | 'dealer' | 'superadmin'`
- `role` in Insert from `'admin' | 'dealer'` to `'admin' | 'dealer' | 'superadmin'`
- `role` in Update from `'admin' | 'dealer'` to `'admin' | 'dealer' | 'superadmin'`
- Add `company_id: string | null` to Row (nullable — superadmin has no company)
- Add `company_id?: string | null` to Insert (optional, defaults to null)
- Add `company_id?: string | null` to Update (optional)

**Edit 2: Add companies table type**

Add the following new table entry to the `Tables` object (add it alphabetically, before `dealer_groups`):

```typescript
companies: {
  Row: {
    id: string
    name: string
    slug: string
    plan: 'starter' | 'pro' | 'enterprise'
    is_active: boolean
    settings: Json
    created_at: string
    updated_at: string
  }
  Insert: {
    id?: string
    name: string
    slug: string
    plan?: 'starter' | 'pro' | 'enterprise'
    is_active?: boolean
    settings?: Json
    created_at?: string
    updated_at?: string
  }
  Update: {
    id?: string
    name?: string
    slug?: string
    plan?: 'starter' | 'pro' | 'enterprise'
    is_active?: boolean
    settings?: Json
    updated_at?: string
  }
  Relationships: []
}
```

Note: Following the existing pattern `@supabase/supabase-js v2.91+` requires `Relationships: []` on all table types.
  </action>
  <verify>
grep "superadmin" src/types/database.types.ts — should find at least 3 matches (Row, Insert, Update for users.role).
grep "companies" src/types/database.types.ts — should find the new companies table block.
grep "company_id" src/types/database.types.ts — should find entries in users type.
  </verify>
  <done>users type has superadmin in role union and company_id field. companies table type exists with Row/Insert/Update/Relationships matching the migration 009 companies table definition.</done>
</task>

<task type="auto">
  <name>Task 2: Add company_id field to all 19 tenant-scoped table types</name>
  <files>src/types/database.types.ts</files>
  <action>
For each of the following 19 tables in database.types.ts, add `company_id` to their Row, Insert, and Update type sections:

**Tables requiring company_id addition:**
1. `dealers` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
2. `dealer_groups` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
3. `dealer_prices` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
4. `dealer_transactions` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
5. `dealer_invoices` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
6. `dealer_favorites` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
7. `orders` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
8. `order_items` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
9. `order_status_history` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
10. `order_documents` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
11. `products` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
12. `categories` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
13. `brands` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
14. `campaigns` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
15. `campaign_products` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
16. `announcements` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
17. `announcement_reads` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
18. `support_messages` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`
19. `product_requests` — Row: `company_id: string`, Insert: `company_id: string`, Update: `company_id?: string`

**Do NOT add company_id to these global tables:**
- `order_statuses`, `order_status_transitions`, `transaction_types`, `faq_categories`, `faq_items`
- These are platform-level lookup tables with no company scoping.

For each table, place `company_id` in alphabetical order within the existing fields (after `campaign_id` or `category_id` where present, or near other `c` fields).

After making all edits, run the TypeScript compiler to verify:
```bash
cd /home/cagr/Masaüstü/bayi-yönetimi && npx tsc --noEmit 2>&1 | head -50
```

If TypeScript errors appear related to company_id being required in Insert types but existing server actions don't pass it: that is expected — the server actions will need the company_id to be injected from the JWT-derived context in Phase 9 (agent infrastructure). For now, make Insert type allow `company_id?: string` (optional with ?) to avoid breaking existing code. The database enforces NOT NULL; the app just needs to pass it explicitly when creating records.

Revise Insert types to use `company_id?: string` (optional) for all 19 tables so existing server actions compile without modification. The RLS will still enforce correct company_id at DB level.
  </action>
  <verify>
Run: cd /home/cagr/Masaüstü/bayi-yönetimi && npx tsc --noEmit 2>&1 | head -30
Expected: No output (zero errors) or only pre-existing errors unrelated to company_id.

Run: grep -c "company_id" src/types/database.types.ts
Expected: 60+ matches (3 per table × 19 tables + companies table + users = ~62).
  </verify>
  <done>All 19 tenant-scoped tables have company_id in Row/Insert/Update types. TypeScript compilation passes with no new errors introduced by this update. Existing server actions continue to compile because Insert types use company_id?: string (optional).</done>
</task>

</tasks>

<verification>
1. grep -c "company_id" src/types/database.types.ts — should be 60+
2. grep "superadmin" src/types/database.types.ts — should appear in users.role union
3. grep -A 5 "companies:" src/types/database.types.ts — should show Row type starting with id, name, slug
4. npx tsc --noEmit — zero new type errors related to company_id fields
5. npm run build — build completes successfully (or with pre-existing non-company_id errors only)
</verification>

<success_criteria>
database.types.ts fully reflects the post-migration schema:
- companies table type complete (Row/Insert/Update/Relationships)
- users.role includes 'superadmin'
- users.company_id present as nullable
- All 19 tenant-scoped tables have company_id field in Row/Insert/Update
- TypeScript compiler produces zero new errors
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-tenant-db-migration/08-05-SUMMARY.md`
</output>
