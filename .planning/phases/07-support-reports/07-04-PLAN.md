---
phase: 07-support-reports
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified: []
autonomous: true
requirements:
  - SUP-01
  - SUP-02
  - SUP-03
  - SUP-04
  - SUP-05
  - SUP-06
  - REP-01
  - REP-02
  - REP-03

must_haves:
  truths:
    - "All Phase 7 requirements pass automated verification"
    - "TypeScript build has zero errors"
    - "Navigation links exist for dealer support and reports pages"
    - "Admin support inbox link exists in admin navigation"
    - "Excel export route returns correct Content-Type header"
    - "xlsx is not imported in any client component"
  artifacts:
    - path: "supabase/migrations/008_support_reports.sql"
      provides: "Phase 7 database migration"
      contains: "support_messages"
    - path: "src/app/(dealer)/support/page.tsx"
      provides: "Dealer support hub"
      min_lines: 20
    - path: "src/app/(dealer)/reports/page.tsx"
      provides: "Dealer spending reports"
      min_lines: 20
    - path: "src/app/(admin)/admin/support/page.tsx"
      provides: "Admin support inbox"
      min_lines: 20
    - path: "src/app/api/reports/spending-export/route.ts"
      provides: "Excel export route handler"
      contains: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
  key_links:
    - from: "src/components/admin/support/message-inbox.tsx"
      to: "src/hooks/use-support-realtime.ts"
      via: "useSupportRealtime import"
      pattern: "useSupportRealtime"
    - from: "src/hooks/use-support-realtime.ts"
      to: "support_messages table"
      via: "postgres_changes subscription"
      pattern: "support_messages"
    - from: "src/components/reports/spending-export-button.tsx"
      to: "/api/reports/spending-export"
      via: "window.location.href"
      pattern: "api/reports/spending-export"
---

<objective>
Verify Phase 7 integration: confirm all requirements are correctly wired end-to-end with automated checks before manual testing.

Purpose: Catch integration issues (missing imports, broken routes, type errors, wrong Content-Type header) that are hard to spot per-plan but visible across the complete phase.
Output: Verified build with all Phase 7 routes and integrations confirmed working.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-support-reports/07-02-SUMMARY.md
@.planning/phases/07-support-reports/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks across all Phase 7 artifacts</name>
  <files></files>
  <action>
Run the following checks in order, fix any failures before moving to the next check:

**Check 1 — TypeScript build:**
```bash
npm run build
```
Expected: Zero errors. If errors exist, fix them. Common causes:
- Missing type import (add to database.types.ts)
- Incorrect prop type mismatch (align component props with query return types)
- Async component missing await (Server Component with async data fetch)

**Check 2 — xlsx bundle safety (CRITICAL):**
```bash
grep -r "import.*xlsx" src --include="*.ts" --include="*.tsx"
```
Expected: Output shows ONLY `src/app/api/reports/spending-export/route.ts`.
If any client component (*.tsx in src/components or src/app) imports xlsx, move the import to the route handler and refactor that component to call the route handler instead.

**Check 3 — Realtime hook wired in admin inbox:**
```bash
grep -r "useSupportRealtime" src --include="*.tsx"
```
Expected: At minimum `src/components/admin/support/message-inbox.tsx` imports and calls `useSupportRealtime`.

**Check 4 — Excel route Content-Type:**
```bash
grep -r "openxmlformats-officedocument.spreadsheetml" src --include="*.ts"
```
Expected: Found in `src/app/api/reports/spending-export/route.ts`. If missing, the file download will be treated as binary blob by the browser.

**Check 5 — Dealer navigation links present:**
```bash
grep -n "support\|reports\|Destek\|Raporlar" src/components/layout/nav-links.tsx
```
Expected: Lines for dealer nav showing /support route (Destek) and /reports route (Raporlar), plus admin nav showing /admin/support route.

**Check 6 — Spending data source correct (not orders table):**
```bash
grep -r "from('orders')" src/lib/queries/spending-reports.ts src/app/api/reports/spending-export/route.ts 2>/dev/null
```
Expected: No output. Spending reports must use dealer_spending_summary or dealer_transactions, not orders.

**Check 7 — Support action exports:**
```bash
grep -n "^export async function" src/lib/actions/support.ts
```
Expected: Lines for sendSupportMessage, submitProductRequest, getSupportMessages, getFaqWithCategories, getDealerProductRequests, getAllSupportMessages, getSupportMessageById, replyToMessage (may vary based on implementation).

**Check 8 — Migration file completeness:**
```bash
grep -c "CREATE TABLE" supabase/migrations/008_support_reports.sql
```
Expected: 4 (support_messages, faq_categories, faq_items, product_requests).

```bash
grep "supabase_realtime" supabase/migrations/008_support_reports.sql
```
Expected: Lines containing ALTER PUBLICATION supabase_realtime ADD TABLE support_messages AND GRANT SELECT ON support_messages TO supabase_realtime.

**Fix any failing checks before proceeding to summary.**

For each failing check:
1. Identify the specific file causing the failure
2. Read that file to understand current state
3. Apply the minimal fix
4. Re-run the specific check to confirm it passes
5. Re-run `npm run build` to confirm no regressions
  </action>
  <verify>
    - npm run build exits with code 0 (zero TypeScript errors)
    - grep for xlsx import returns only route.ts
    - useSupportRealtime is used in message-inbox.tsx
    - spending-export/route.ts contains correct Content-Type string
    - nav-links.tsx contains Destek and Raporlar entries
    - spending-reports.ts does not query from('orders')
    - 008_support_reports.sql has 4 CREATE TABLE statements and supabase_realtime setup
  </verify>
  <done>All automated checks pass; Phase 7 integration verified</done>
</task>

</tasks>

<verification>
Phase 7 integration is verified when ALL of the following pass:
1. `npm run build` — zero errors
2. xlsx imported only in route.ts — no client bundle bloat
3. useSupportRealtime wired in admin inbox — realtime notification will fire
4. Correct Content-Type in route handler — browser triggers download
5. Navigation links present — dealer can discover support and reports pages
6. Spending data from correct source — reports show financial accuracy
7. Migration has all 4 tables + realtime setup — DB schema ready to apply
</verification>

<success_criteria>
All Phase 7 requirements are traceable from code to requirement:
- SUP-01/02: sendSupportMessage Server Action + getSupportMessages query + /support dealer page
- SUP-03: getFaqWithCategories query + FaqCategoryList component + /support/faq page
- SUP-04: submitProductRequest Server Action + ProductRequestForm + /support/product-requests page
- SUP-05: useSupportRealtime hook with postgres_changes on support_messages INSERT
- SUP-06: replyToMessage Server Action (atomic update) + Admin pages + FaqManager component
- REP-01: getDealerMonthlySpending query + SpendingTrendChart + /reports page
- REP-02: getSpendingComparison query + PeriodComparison component
- REP-03: xlsx Route Handler at /api/reports/spending-export + SpendingExportButton

Build passes. Phase 7 is ready for manual end-to-end testing.
</success_criteria>

<output>
After completion, create `.planning/phases/07-support-reports/07-04-SUMMARY.md`
</output>
