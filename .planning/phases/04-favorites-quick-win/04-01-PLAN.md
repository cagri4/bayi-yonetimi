---
phase: 04-favorites-quick-win
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/005_dealer_favorites.sql
  - src/lib/actions/favorites.ts
autonomous: true

must_haves:
  truths:
    - "Dealer favorites are stored in database"
    - "Dealer can only see own favorites (RLS enforced)"
    - "Toggle favorite works (add and remove)"
    - "Get favorites returns products with fresh pricing"
  artifacts:
    - path: "supabase/migrations/005_dealer_favorites.sql"
      provides: "dealer_favorites table with RLS"
      contains: "CREATE TABLE dealer_favorites"
    - path: "src/lib/actions/favorites.ts"
      provides: "Server Actions for favorites CRUD"
      exports: ["toggleFavorite", "getFavoriteProducts", "getFavoriteIds"]
  key_links:
    - from: "src/lib/actions/favorites.ts"
      to: "dealer_favorites table"
      via: "Supabase client queries"
      pattern: "from\\('dealer_favorites'\\)"
    - from: "dealer_favorites RLS policies"
      to: "dealers.user_id"
      via: "RLS policy check"
      pattern: "user_id = \\(SELECT auth.uid\\(\\)\\)"
---

<objective>
Create database foundation and Server Actions for dealer favorites feature.

Purpose: Enable dealers to save favorite products with multi-tenant isolation (RLS) and provide type-safe Server Actions for toggling and listing favorites.
Output: Migration file with dealer_favorites table, RLS policies, indexes; Server Actions for toggle/list operations.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-favorites-quick-win/04-RESEARCH.md

# Existing patterns to follow
@supabase/migrations/001_initial_schema.sql
@src/lib/actions/catalog.ts
@src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dealer_favorites database migration</name>
  <files>supabase/migrations/005_dealer_favorites.sql</files>
  <action>
Create SQL migration for dealer_favorites table following v1 patterns from 001_initial_schema.sql:

1. Create dealer_favorites junction table:
   - id UUID PRIMARY KEY DEFAULT uuid_generate_v4()
   - dealer_id UUID NOT NULL REFERENCES dealers(id) ON DELETE CASCADE
   - product_id UUID NOT NULL REFERENCES products(id) ON DELETE CASCADE
   - created_at TIMESTAMPTZ DEFAULT NOW()
   - UNIQUE constraint on (dealer_id, product_id)

2. Create indexes for query performance:
   - Composite index on (dealer_id, product_id) - critical for toggle checks
   - Index on product_id - for "most favorited products" queries (future)

3. Enable RLS and create policies following v1 pattern:
   - "Dealers can view own favorites" - SELECT using (SELECT auth.uid()) pattern
   - "Dealers can add favorites" - INSERT WITH CHECK using same pattern
   - "Dealers can remove favorites" - DELETE using same pattern

Critical: Use `(SELECT auth.uid())` wrapped pattern for 94-99% RLS performance improvement (per Supabase docs).
  </action>
  <verify>
Run: `npx supabase db push --dry-run` to verify migration syntax is valid.
Check migration file contains: CREATE TABLE, CREATE INDEX, ENABLE ROW LEVEL SECURITY, CREATE POLICY.
  </verify>
  <done>
Migration file 005_dealer_favorites.sql exists with:
- dealer_favorites table with all columns and constraints
- Composite index on (dealer_id, product_id)
- RLS enabled with 3 policies (SELECT, INSERT, DELETE)
- All policies use wrapped auth.uid() pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: Create favorites Server Actions</name>
  <files>src/lib/actions/favorites.ts</files>
  <action>
Create Server Actions file following v1 pattern from catalog.ts:

1. Add 'use server' directive at top

2. Create toggleFavorite(productId: string): Promise&lt;boolean&gt;
   - Get authenticated user via supabase.auth.getUser()
   - Get dealer ID from dealers table (WHERE user_id = user.id)
   - Check if favorite exists (SELECT from dealer_favorites)
   - If exists: DELETE and return false
   - If not exists: INSERT and return true
   - Call revalidatePath('/favorites') and revalidatePath('/catalog')

3. Create getFavoriteProducts(): Promise&lt;FavoriteProduct[]&gt;
   - Define FavoriteProduct interface matching CatalogProduct from catalog.ts
   - Get dealer info including dealer_group discount_percent
   - Query dealer_favorites with product join:
     ```
     .from('dealer_favorites')
     .select(`product:products(id, code, name, description, base_price, stock_quantity, low_stock_threshold, image_url, category:categories(id, name, slug), brand:brands(id, name, slug))`)
     .eq('dealer_id', dealer.id)
     .order('created_at', { ascending: false })
     ```
   - Get dealer_prices for custom pricing
   - Calculate dealer_price using same logic as getCatalogProducts:
     - Check for custom_price override first
     - Otherwise apply group discount_percent

4. Create getFavoriteIds(): Promise&lt;string[]&gt;
   - Simple query returning just product IDs for catalog page hydration
   - Used to mark which products are favorited in catalog view

Export all three functions and the FavoriteProduct interface.
  </action>
  <verify>
Run: `npm run build` to verify TypeScript compilation.
Check exports exist: toggleFavorite, getFavoriteProducts, getFavoriteIds, FavoriteProduct.
  </verify>
  <done>
src/lib/actions/favorites.ts exists with:
- toggleFavorite returning boolean (true = added, false = removed)
- getFavoriteProducts returning array with fresh dealer pricing
- getFavoriteIds returning array of product IDs
- All functions handle unauthorized state gracefully
- revalidatePath called after mutations
  </done>
</task>

</tasks>

<verification>
1. Migration syntax valid: `npx supabase db push --dry-run` succeeds
2. TypeScript compiles: `npm run build` succeeds
3. Server Actions exported correctly: imports work in test file
4. RLS policies use wrapped auth.uid() pattern
</verification>

<success_criteria>
- dealer_favorites table schema ready for deployment
- Server Actions provide complete CRUD for favorites
- Pricing calculation matches existing catalog logic
- Multi-tenant isolation via RLS policies
</success_criteria>

<output>
After completion, create `.planning/phases/04-favorites-quick-win/04-01-SUMMARY.md`
</output>
